# TypeScript Migration Phase 4: CLI + 集成测试 + 发布 Implementation Plan

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** 完成界面层迁移，集成测试，发布可用版本

**Milestone ref:** milestone-14 from claude-progress.json

**Architecture:** 迁移 CLI 层（Theme, Banner, Spinner, StageIndicator, StreamRenderer, ToolDisplay, Display, Completer, PaginatedSelector, App），创建入口点，配置全局命令

**Tech Stack:** chalk, marked, marked-terminal, readline, ora

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `src/cli/theme.ts` | Create | chalk 样式常量 |
| `src/cli/banner.ts` | Create | ASCII art + 信息表格 |
| `src/cli/spinner.ts` | Create | Braille 动画 spinner |
| `src/cli/stage-indicator.ts` | Create | 阶段指示器 |
| `src/cli/stream-renderer.ts` | Create | 安全边界 Markdown 流式渲染 |
| `src/cli/tool-display.ts` | Create | 工具调用面板 |
| `src/cli/display.ts` | Create | 显示工具 |
| `src/cli/completer.ts` | Create | 斜杠命令补全 |
| `src/cli/paginated-selector.ts` | Create | 分页选择器 |
| `src/cli/app.ts` | Create | 主 REPL 类 |
| `src/entrypoints/cli.ts` | Modify | 入口文件 |
| `tests/cli.test.ts` | Create | CLI 层测试 |

---

### Task 1: 迁移 Theme

**Files:**

- Create: `src/cli/theme.ts`

**TDD_EVIDENCE:** `tsc --noEmit` 应无类型错误

- [ ] **Step 1: 创建 src/cli/theme.ts**

```typescript
import chalk from 'chalk'

export const THEME = {
  // Markdown styles
  markdown: {
    h1: chalk.bold.cyan,
    h2: chalk.bold.white,
    h3: chalk.blue,
    code: chalk.green,
    codeBlock: chalk.bgGray,
    link: chalk.blue.underline,
    em: chalk.italic.magenta,
    strong: chalk.bold.yellow,
    blockquote: chalk.gray,
  },
  // Tool styles
  tool: {
    border: chalk.gray,
    name: chalk.bold.cyan,
    success: chalk.green,
    error: chalk.red,
  },
  // Spinner styles
  spinner: {
    active: chalk.blue,
    done: chalk.green,
    failed: chalk.red,
  },
  // Stage styles
  stage: {
    name: chalk.dim.white,
    time: chalk.dim.cyan,
    detail: chalk.dim.gray,
  },
} as const
```

- [x] **Step 2: Commit**

```bash
git add src/cli/theme.ts
git commit -m "feat: migrate theme with chalk styles"
```

---

### Task 2: 迁移 Banner

**Files:**

- Create: `src/cli/banner.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::Banner` 应通过

- [ ] **Step 1: 创建 src/cli/banner.ts**

```typescript
import chalk from 'chalk'

const BRIX_ASCII = `
 ██████╗ ██████╗ ██╗██╗  ██╗
 ██╔══██╗██╔══██╗██║╚██╗██╔╝
 ██████╔╝██████╔╝██║ ╚███╔╝
 ██╔══██╗██╔══██╗██║ ██╔██╗
 ██████╔╝██║  ██║██║██╔╝ ██╗
 ╚═════╝ ╚═╝  ╚═╝╚═╝╚═╝  ╚═╝
`

export function showBanner(model: string, version: string, cwd: string): void {
  console.log(chalk.bold.cyan(BRIX_ASCII))
  console.log(chalk.dim('  BRIX — Personal AI Agent\n'))
  console.log(chalk.dim('  Model:    ') + chalk.white(model))
  console.log(chalk.dim('  Version:  ') + chalk.white(version))
  console.log(chalk.dim('  Directory: ') + chalk.white(cwd))
  console.log()
  console.log(chalk.dim('  Type /help for commands · Ctrl+C to exit\n'))
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/banner.ts
git commit -m "feat: migrate banner with chalk"
```

---

### Task 3: 迁移 Spinner

**Files:**

- Create: `src/cli/spinner.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::Spinner` 应通过

- [ ] **Step 1: 创建 src/cli/spinner.ts**

```typescript
import chalk from 'chalk'

const BRAILLE_FRAMES = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏']

export class Spinner {
  private label: string
  private frameIdx: number = 0
  private startTime: number = 0
  private running: boolean = false
  private timer: ReturnType<typeof setInterval> | null = null

  constructor(label: string = 'Thinking...') {
    this.label = label
  }

  private renderFrame(): string {
    const elapsed = (Date.now() - this.startTime) / 1000
    const frame = BRAILLE_FRAMES[this.frameIdx % BRAILLE_FRAMES.length]
    return `  ${chalk.blue(frame)} ${chalk.dim(this.label)}  ${chalk.dim.cyan(elapsed.toFixed(1) + 's')}`
  }

  start(): void {
    this.running = true
    this.startTime = Date.now()
    this.timer = setInterval(() => {
      this.frameIdx++
      process.stdout.write('\r' + this.renderFrame())
    }, 100)
  }

  stop(): void {
    this.running = false
    if (this.timer) {
      clearInterval(this.timer)
      this.timer = null
    }
    // 清除行
    process.stdout.write('\r' + ' '.repeat(80) + '\r')
  }

  updateLabel(label: string): void {
    this.label = label
  }

  finish(label: string = 'Done'): void {
    this.stop()
    const elapsed = (Date.now() - this.startTime) / 1000
    console.log(`  ${chalk.green('✓')} ${label}  ${chalk.dim(elapsed.toFixed(1) + 's')}`)
  }

  fail(label: string = 'Failed'): void {
    this.stop()
    const elapsed = (Date.now() - this.startTime) / 1000
    console.log(`  ${chalk.red('✗')} ${label}  ${chalk.dim(elapsed.toFixed(1) + 's')}`)
  }
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/spinner.ts
git commit -m "feat: migrate spinner with Braille animation"
```

---

### Task 4: 迁移 Stage Indicator

**Files:**

- Create: `src/cli/stage-indicator.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::StageIndicator` 应通过

- [ ] **Step 1: 创建 src/cli/stage-indicator.ts**

```typescript
import { Spinner } from './spinner.js'

const STAGE_LABELS: Record<string, string> = {
  Memory: 'Loading memory...',
  Intent: 'Classifying intent...',
  Complexity: 'Evaluating complexity...',
  Route: 'Selecting model...',
  Planning: 'Planning...',
}

export class StageIndicator {
  private spinner: Spinner
  private finished: boolean = false

  constructor(label: string = 'Thinking...') {
    this.spinner = new Spinner(label)
    this.spinner.start()
  }

  update(stage: string): void {
    if (this.finished) return
    const label = STAGE_LABELS[stage] || 'Working...'
    this.spinner.updateLabel(label)
  }

  finish(): void {
    if (this.finished) return
    this.finished = true
    this.spinner.stop()
  }

  stop_silent(): void {
    if (this.finished) return
    this.finished = true
    this.spinner.stop()
  }
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/stage-indicator.ts
git commit -m "feat: migrate stage indicator with spinner"
```

---

### Task 5: 迁移 Stream Renderer

**Files:**

- Create: `src/cli/stream-renderer.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::StreamRenderer` 应通过

- [ ] **Step 1: 创建 src/cli/stream-renderer.ts**

```typescript
import chalk from 'chalk'
import { marked } from 'marked'
import TerminalRenderer from 'marked-terminal'

// 配置 marked 使用 terminal renderer
marked.setOptions({ renderer: new TerminalRenderer() } as any)

const BRAILLE_FRAMES = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏']

export class StreamRenderer {
  private pending: string = ''
  private rendered: string = ''
  private marker: string
  private lastDeltaTime: number = 0
  private activityTimer: ReturnType<typeof setTimeout> | null = null
  private indicatorFrame: number = 0
  private indicatorLabel: string = 'Waiting for tool call...'

  constructor(marker: string = chalk.green('  ⏺ ')) {
    this.marker = marker
  }

  pushDelta(delta: string): void {
    this.lastDeltaTime = Date.now()
    this.pending += delta

    // 查找安全边界
    const boundary = this.findSafeBoundary(this.pending)
    if (boundary !== null) {
      this.rendered += this.pending.slice(0, boundary)
      this.pending = this.pending.slice(boundary)
      this.updateDisplay()
    }

    // 重置 activity indicator
    this.resetActivityIndicator()
  }

  private findSafeBoundary(text: string): number | null {
    // 在代码块结束后
    const codeFenceEnd = text.lastIndexOf('```')
    if (codeFenceEnd > 0 && codeFenceEnd < text.length - 3) {
      const afterFence = text.indexOf('\n', codeFenceEnd + 3)
      if (afterFence !== -1) return afterFence + 1
    }

    // 在空行后
    const lastBlankLine = text.lastIndexOf('\n\n')
    if (lastBlankLine !== -1) return lastBlankLine + 2

    // 在非代码块的换行后
    const lastNewline = text.lastIndexOf('\n')
    if (lastNewline !== -1) return lastNewline + 1

    return null
  }

  private updateDisplay(): void {
    if (this.rendered) {
      const output = marked(this.rendered)
      process.stdout.write(this.marker + output)
    }
  }

  private resetActivityIndicator(): void {
    if (this.activityTimer) {
      clearTimeout(this.activityTimer)
    }
    this.activityTimer = setTimeout(() => {
      this.showActivityIndicator()
    }, 800)
  }

  private showActivityIndicator(): void {
    const frame = BRAILLE_FRAMES[this.indicatorFrame % BRAILLE_FRAMES.length]
    process.stdout.write(`\r  ${chalk.dim(frame)} ${chalk.dim(this.indicatorLabel)}`)
    this.indicatorFrame++
    this.activityTimer = setTimeout(() => this.showActivityIndicator(), 100)
  }

  flush(): void {
    if (this.activityTimer) {
      clearTimeout(this.activityTimer)
      this.activityTimer = null
    }
    // 清除 activity indicator
    process.stdout.write('\r' + ' '.repeat(80) + '\r')

    if (this.pending) {
      this.rendered += this.pending
      this.pending = ''
      this.updateDisplay()
    }
  }

  getOutput(): string {
    return this.rendered
  }
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/stream-renderer.ts
git commit -m "feat: migrate stream renderer with safe boundary rendering"
```

---

### Task 6: 迁移 Tool Display

**Files:**

- Create: `src/cli/tool-display.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::ToolDisplay` 应通过

- [ ] **Step 1: 创建 src/cli/tool-display.ts**

```typescript
import chalk from 'chalk'
import { Spinner } from './spinner.js'

const TOOL_ICONS: Record<string, string> = {
  bash: '⚡',
  file_read: '📄',
  file_write: '✏️',
  file_edit: '📝',
  web_search: '🔍',
  calculator: '🧮',
  weather: '🌤️',
}

export class ToolDisplay {
  private activeSpinner: Spinner | null = null

  showToolStart(toolName: string, toolInput: Record<string, unknown>): void {
    const icon = TOOL_ICONS[toolName] || '🔧'
    const detail = this.formatDetail(toolName, toolInput)

    console.log(chalk.dim('  ⏺ Calling tools...'))
    console.log(chalk.gray('┌─ ') + chalk.bold.cyan(`${icon} ${toolName}`) + chalk.gray(' ─'))
    console.log(chalk.gray('│ ') + detail)
    console.log(chalk.gray('└─' + '─'.repeat(40)))
  }

  showToolResult(toolName: string, result: string, elapsedMs: number, isError: boolean = false): void {
    this.stopThinking()
    const icon = TOOL_ICONS[toolName] || '🔧'
    const statusIcon = isError ? chalk.red('✗') : chalk.green('✓')
    const elapsed = chalk.dim.cyan(`${elapsedMs.toFixed(0)}ms`)

    const preview = result.slice(0, 200).replace(/\n/g, ' ')
    const truncated = result.length > 200 ? '...' : ''

    let line = `  ⎯ ${chalk.dim(icon)} ${chalk.bold.cyan(toolName)} ${statusIcon} ${elapsed}`
    if (isError) {
      line += `  ${chalk.red(preview + truncated)}`
    }
    console.log(line)
    this.startThinking()
  }

  startThinking(): void {
    if (!this.activeSpinner) {
      this.activeSpinner = new Spinner('Thinking...')
      this.activeSpinner.start()
    }
  }

  stopThinking(): void {
    if (this.activeSpinner) {
      this.activeSpinner.stop()
      this.activeSpinner = null
    }
  }

  cleanup(): void {
    this.stopThinking()
  }

  private formatDetail(toolName: string, toolInput: Record<string, unknown>): string {
    if (!toolInput || typeof toolInput !== 'object') {
      return chalk.dim(JSON.stringify(toolInput).slice(0, 150))
    }

    switch (toolName) {
      case 'bash':
        return chalk.white.bgGray(` $ ${toolInput.command || ''} `)
      case 'file_read':
        return `📄 Reading ${toolInput.path || ''}`
      case 'file_write':
        return `✏️ Writing ${toolInput.path || ''} (${(toolInput.content as string || '').split('\n').length} lines)`
      case 'file_edit':
        return `📝 Editing ${toolInput.path || ''}`
      case 'web_search':
        return `🔍 Searching: ${toolInput.query || ''}`
      default:
        return chalk.dim(JSON.stringify(toolInput).slice(0, 150))
    }
  }
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/tool-display.ts
git commit -m "feat: migrate tool display with panels and thinking spinner"
```

---

### Task 7: 迁移 Display + Completer

**Files:**

- Create: `src/cli/display.ts`
- Create: `src/cli/completer.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::Completer` 应通过

- [ ] **Step 1: 创建 src/cli/display.ts**

```typescript
import chalk from 'chalk'
import type { Message } from '../types.js'

export function formatResponse(content: string): string {
  return content
}

export function renderHistory(messages: Message[]): void {
  for (const msg of messages) {
    if (msg.role === 'user') {
      console.log(chalk.cyan.bold('  ❯ ') + msg.content)
    } else if (msg.role === 'assistant') {
      console.log(chalk.green('  ⏺ ') + msg.content)
    } else if (msg.role === 'system') {
      console.log(chalk.dim('  [system] ') + msg.content)
    }
    console.log()
  }
}
```

- [ ] **Step 2: 创建 src/cli/completer.ts**

```typescript
import { COMMANDS } from '../capability/basics/commands.js'

export function createCompleter(): (line: string) => [string[], string] {
  return (line: string): [string[], string] => {
    if (!line.startsWith('/')) {
      return [[], line]
    }

    if (line.includes(' ')) {
      return [[], line]
    }

    const lowerLine = line.toLowerCase()
    const hits = COMMANDS
      .map(([cmd]) => cmd.split(' ')[0]) // "/resume [id]" -> "/resume"
      .filter(cmd => cmd.toLowerCase().startsWith(lowerLine))

    return [hits.length ? hits : COMMANDS.map(([cmd]) => cmd.split(' ')[0]), line]
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/cli/display.ts src/cli/completer.ts
git commit -m "feat: migrate display and completer"
```

---

### Task 8: 迁移 Paginated Selector

**Files:**

- Create: `src/cli/paginated-selector.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::PaginatedSelector` 应通过

- [ ] **Step 1: 创建 src/cli/paginated-selector.ts**

```typescript
import * as readline from 'readline'

export async function paginatedSelect<T>(
  items: T[],
  formatItem: (item: T, idx: number) => string,
  pageSize: number = 10,
  title: string = 'Select'
): Promise<T | null> {
  if (items.length === 0) return null

  let currentPage = 0
  let selectedIndex = 0
  const totalPages = Math.ceil(items.length / pageSize)

  const render = () => {
    console.clear()
    console.log(`  ${title}\n`)

    const start = currentPage * pageSize
    const pageItems = items.slice(start, start + pageSize)

    for (let i = 0; i < pageItems.length; i++) {
      const globalIdx = start + i + 1
      const label = formatItem(pageItems[i], start + i)
      if (i === selectedIndex) {
        console.log(`  \x1b[36m> ${globalIdx}. ${label}\x1b[0m`)
      } else {
        console.log(`    ${globalIdx}. ${label}`)
      }
    }

    // 补齐空行
    for (let i = pageItems.length; i < pageSize; i++) {
      console.log()
    }

    console.log(`\n  第 ${currentPage + 1}/${totalPages} 页 (共 ${items.length} 项)`)
    console.log('  [↑↓] 上下  [←→] 翻页  [Enter] 确认  [Esc] 取消')
  }

  return new Promise((resolve) => {
    const rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
    })

    // 禁用 echo
    if (process.stdin.isTTY) {
      process.stdin.setRawMode(true)
    }

    render()

    const onKey = (key: Buffer) => {
      const ch = key.toString()

      switch (ch) {
        case '\x1b[A': // 上
          if (selectedIndex > 0) {
            selectedIndex--
          } else if (currentPage > 0) {
            currentPage--
            selectedIndex = Math.min(pageSize - 1, items.length - currentPage * pageSize - 1)
          }
          break
        case '\x1b[B': // 下
          const maxIdx = Math.min(pageSize - 1, items.length - currentPage * pageSize - 1)
          if (selectedIndex < maxIdx) {
            selectedIndex++
          } else if (currentPage < totalPages - 1) {
            currentPage++
            selectedIndex = 0
          }
          break
        case '\x1b[C': // 右
        case '\x1b[5~': // PageUp
          if (currentPage < totalPages - 1) {
            currentPage++
            selectedIndex = Math.min(selectedIndex, items.length - currentPage * pageSize - 1)
          }
          break
        case '\x1b[D': // 左
        case '\x1b[6~': // PageDown
          if (currentPage > 0) {
            currentPage--
            selectedIndex = Math.min(selectedIndex, items.length - currentPage * pageSize - 1)
          }
          break
        case '\r': // Enter
          cleanup()
          resolve(items[currentPage * pageSize + selectedIndex])
          return
        case '\x1b': // Esc
        case 'q':
          cleanup()
          resolve(null)
          return
        case '\x03': // Ctrl+C
          cleanup()
          resolve(null)
          return
        default:
          // 数字键快速跳转
          const num = parseInt(ch)
          if (num >= 1 && num <= 9) {
            const idx = num - 1
            if (idx < items.length - currentPage * pageSize) {
              selectedIndex = idx
            }
          }
          break
      }

      render()
    }

    const cleanup = () => {
      process.stdin.removeListener('data', onKey)
      if (process.stdin.isTTY) {
        process.stdin.setRawMode(false)
      }
      rl.close()
    }

    process.stdin.on('data', onKey)
  })
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/paginated-selector.ts
git commit -m "feat: migrate paginated selector with key bindings"
```

---

### Task 9: 迁移 App (主 REPL)

**Files:**

- Create: `src/cli/app.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts::BrixCLI` 应通过

- [ ] **Step 1: 创建 src/cli/app.ts**

```typescript
import * as readline from 'readline'
import chalk from 'chalk'
import type { BrixConfig } from '../config/loader.js'
import { loadConfig } from '../config/loader.js'
import { createMemoryProvider } from '../memory/provider.js'
import type { MemoryProvider } from '../memory/types.js'
import { LLMClient } from '../infra/llm-client.js'
import { ToolRunner } from '../capability/runner.js'
import { CalculatorTool } from '../capability/tools/calculator.js'
import { WeatherTool } from '../capability/tools/weather.js'
import { FileReadTool } from '../capability/tools/file-read.js'
import { FileWriteTool } from '../capability/tools/file-write.js'
import { FileEditTool } from '../capability/tools/file-edit.js'
import { FlowLog } from '../log/flow.js'
import { flushLog } from '../log/writer.js'
import { HookRegistry } from '../hooks/registry.js'
import { classifyIntent } from '../router/intent.js'
import { evaluate_complexity } from '../router/complexity.js'
import { selectModel } from '../router/model-router.js'
import { StateMachineOrchestrator } from '../orchestrator/state-machine.js'
import type { OrchestratorContext } from '../orchestrator/engine.js'
import { showBanner } from './banner.js'
import { createCompleter } from './completer.js'
import { renderHistory } from './display.js'
import { StageIndicator } from './stage-indicator.js'
import { StreamRenderer } from './stream-renderer.js'
import { ToolDisplay } from './tool-display.js'
import { paginatedSelect } from './paginated-selector.js'
import { COMMANDS } from '../capability/basics/commands.js'
import { getRecentLogs, getLogDetail } from '../capability/basics/logs.js'
import { loadSoul, loadUser } from '../capability/basics/memory-files.js'
import { listSessions, get_session_by_prefix, resumeSession } from '../capability/basics/sessions.js'

export class BrixCLI {
  private config: BrixConfig
  private memory: MemoryProvider
  private llmClient: LLMClient
  private toolRunner: ToolRunner
  private orchestrator: StateMachineOrchestrator
  private hooks: HookRegistry
  private logPath: string

  constructor(config?: BrixConfig) {
    this.config = config ?? loadConfig()
    this.memory = createMemoryProvider(this.config.memory.data_dir, this.config.memory.max_context_tokens)
    this.llmClient = new LLMClient(this.config)
    this.toolRunner = new ToolRunner()
    this.orchestrator = new StateMachineOrchestrator()
    this.hooks = new HookRegistry()
    this.logPath = `${this.config.memory.data_dir}/../log/data/brix.jsonl`

    this.registerTools()
  }

  private registerTools(): void {
    this.toolRunner.register(new CalculatorTool())
    this.toolRunner.register(new WeatherTool())
    this.toolRunner.register(new FileReadTool())
    this.toolRunner.register(new FileWriteTool(this.config.memory.data_dir))
    this.toolRunner.register(new FileEditTool(this.config.memory.data_dir))
  }

  async run(): Promise<void> {
    showBanner(this.config.routing.default_model, '0.1.0', process.cwd())

    const rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
      completer: createCompleter(),
    })

    const prompt = () => {
      rl.question(chalk.cyan.bold('  ❯ '), async (input) => {
        const trimmed = input.trim()
        if (!trimmed) {
          prompt()
          return
        }

        try {
          await this.handleInput(trimmed)
        } catch (e) {
          console.error(chalk.red(`Error: ${e instanceof Error ? e.message : String(e)}`))
        }

        prompt()
      })
    }

    prompt()
  }

  private async handleInput(input: string): Promise<void> {
    // 处理 slash 命令
    if (input.startsWith('/')) {
      await this.handleCommand(input)
      return
    }

    // 处理普通对话
    await this.handleChat(input)
  }

  private async handleCommand(input: string): Promise<void> {
    const [cmd, ...args] = input.split(' ')
    const arg = args.join(' ')

    switch (cmd) {
      case '/help':
        this.showHelp()
        break
      case '/quit':
        this.memory.saveSession()
        console.log(chalk.dim('\nGoodbye.'))
        process.exit(0)
      case '/clear':
        this.memory.clearSession()
        console.log(chalk.dim('Session cleared.'))
        break
      case '/model':
        console.log(chalk.dim('Current model: ') + chalk.white(this.config.routing.default_model))
        break
      case '/history':
        const messages = this.memory.getContextMessages('')
        renderHistory(messages)
        break
      case '/resume':
        await this.handleResume(arg)
        break
      case '/soul':
        const soul = loadSoul(this.memory)
        console.log(soul || chalk.dim('No soul.md found.'))
        break
      case '/user':
        const user = loadUser(this.memory)
        console.log(user || chalk.dim('No user.md found.'))
        break
      case '/log':
        await this.handleLog(arg)
        break
      default:
        console.log(chalk.dim(`Unknown command: ${cmd}`))
    }
  }

  private showHelp(): void {
    console.log(chalk.bold('\n  Commands:\n'))
    for (const [cmd, desc] of COMMANDS) {
      console.log(`    ${chalk.cyan(cmd.padEnd(20))} ${chalk.dim(desc)}`)
    }
    console.log()
  }

  private async handleResume(arg: string): Promise<void> {
    const sessions = listSessions(this.memory)
    if (sessions.length === 0) {
      console.log(chalk.dim('No sessions found.'))
      return
    }

    if (arg) {
      // 恢复指定 session
      const success = await resumeSession(this.memory, arg)
      if (success) {
        console.log(chalk.dim(`Session ${arg} resumed.`))
      } else {
        console.log(chalk.dim(`Session ${arg} not found.`))
      }
      return
    }

    // 显示 session 列表
    const selected = await paginatedSelect(
      sessions,
      (session, idx) => `${session.id.slice(0, 8)}... (${session.message_count} messages)`,
      10,
      'Select a session to resume'
    )

    if (selected) {
      await resumeSession(this.memory, selected.id)
      console.log(chalk.dim(`Session ${selected.id} resumed.`))
    }
  }

  private async handleLog(arg: string): Promise<void> {
    if (arg) {
      // 显示详细日志
      const detail = getLogDetail(this.logPath, arg)
      if (detail) {
        console.log(detail)
      } else {
        console.log(chalk.dim(`Log entry ${arg} not found.`))
      }
      return
    }

    // 显示最近日志
    const logs = getRecentLogs(this.logPath, 10)
    if (logs.length === 0) {
      console.log(chalk.dim('No logs found.'))
      return
    }

    for (const entry of logs) {
      const trace = chalk.cyan((entry.trace_id as string).slice(0, 8))
      const input = (entry.input as string).slice(0, 50)
      const ms = chalk.dim.cyan(`${(entry.total_ms as number).toFixed(0)}ms`)
      console.log(`  ${trace} ${input} ${ms}`)
    }
  }

  private async handleChat(input: string): Promise<void> {
    // 创建 FlowLog
    const log = new FlowLog(input)
    this.hooks.bindLog(log)

    // Stage indicator
    const stage = new StageIndicator('Thinking...')

    try {
      // Memory
      stage.update('Memory')
      const systemPrompt = this.memory.buildSystemPrompt()
      const contextMessages = this.memory.getContextMessages(systemPrompt)

      // Intent
      stage.update('Intent')
      const intent = await classifyIntent(input, this.llmClient, this.config.routing.default_model, this.hooks)
      log.step('router', { intent })

      // Complexity
      stage.update('Complexity')
      const complexity = evaluate_complexity(input)
      log.step('router', { complexity })

      // Route
      stage.update('Route')
      const model = selectModel(intent, complexity, this.config.routing.default_model, this.config.routing.fallback_model)
      log.setModel(model)
      log.step('router', { model })

      // Planning
      stage.update('Planning')
      stage.finish()

      // Orchestrator
      const context: OrchestratorContext = {
        history: contextMessages,
        memory: {},
        toolRunner: this.toolRunner,
        llmClient: this.llmClient,
        model,
        hooks: this.hooks,
      }

      const toolDisplay = new ToolDisplay()
      const streamRenderer = new StreamRenderer()

      // 流式处理
      for await (const event of this.orchestrator.runStream(input, context)) {
        if (event.type === 'text_delta') {
          streamRenderer.pushDelta(event.text)
        } else if (event.type === 'tool_call') {
          toolDisplay.showToolStart(event.name, event.input)
        } else if (event.type === 'tool_result') {
          toolDisplay.showToolResult(event.name, event.result, event.ms, event.is_error)
        }
      }

      streamRenderer.flush()
      toolDisplay.cleanup()

      // 保存消息
      this.memory.addMessage('user', input)
      this.memory.addMessage('assistant', streamRenderer.getOutput())
      await this.memory.saveSession()

      // 记录日志
      log.step('complete', { output_length: streamRenderer.getOutput().length })
      flushLog(this.logPath, log.finish())

    } catch (e) {
      stage.fail('Error')
      log.setError(e instanceof Error ? e.message : String(e))
      flushLog(this.logPath, log.finish())
      throw e
    }
  }
}
```

- [x] **Step 2: Commit**

```bash
git add src/cli/app.ts
git commit -m "feat: migrate main REPL with all components"
```

---

### Task 10: 创建入口点

**Files:**

- Modify: `src/entrypoints/cli.ts`

**TDD_EVIDENCE:** `bun run src/entrypoints/cli.ts` 应能启动 REPL

- [x] **Step 1: 更新入口文件**

```typescript
#!/usr/bin/env bun
import 'dotenv/config'
import { BrixCLI } from '../cli/app.js'

async function main() {
  const cli = new BrixCLI()
  await cli.run()
}

main().catch(console.error)
```

- [x] **Step 2: Commit**

```bash
git add src/entrypoints/cli.ts
git commit -m "feat: update CLI entry point with full REPL"
```

---

### Task 11: 配置全局命令

**Files:**

- Modify: `.gitignore`

**TDD_EVIDENCE:** `bun link` 应能注册全局命令

- [ ] **Step 1: 更新 .gitignore**

```gitignore
# 新增
node_modules/
dist/
*.js.map
*.d.ts

# 保留（路径更新）
.env
src/memory/data/
src/log/data/
*.pyc
__pycache__/
.venv/
```

- [ ] **Step 2: 构建**

Run: `bun run build`
Expected: 生成 dist/cli.js

- [ ] **Step 3: 注册全局命令**

Run: `bun link`
Expected: 注册 brix 命令

- [ ] **Step 4: 验证**

Run: `which brix`
Expected: 输出路径

- [ ] **Step 5: Commit**

```bash
git add .gitignore
git commit -m "chore: update gitignore and configure global command"
```

---

### Task 12: 更新 CLAUDE.md

**Files:**

- Modify: `CLAUDE.md`

**TDD_EVIDENCE:** CLAUDE.md 更新完成

- [ ] **Step 1: 更新 CLAUDE.md**

```markdown
# Brix 开发约束

## 模块化（最高优先级）

所有层通过 TypeScript 接口通信，禁止跨层直接 import 内部实现。

- `cli/` → 依赖各层的接口定义，不 import 内部实现
- `orchestrator/` → 通过 `OrchestratorEngine` 接口
- `memory/` → 通过 `MemoryProvider` 接口
- `capability/` → 通过 `Tool` 接口 + `ToolRunner`
- `router/` → 通过函数调用（classifyIntent, selectModel）
- `infra/` → 通过 `LLMClient` 类

替换任何一层只需满足同一接口，不改动调用方。

## 数据与逻辑分离

运行时数据放各模块的 `data/` 子目录（如 `src/memory/data/`、`src/log/data/`），`.ts` 是逻辑层，`.md`/`.json` 是数据层。各 `data/` 目录加入 `.gitignore`。

## 代码风格

- 中文注释和文档
- TypeScript strict 模式
- 异步优先 (async/await)，fail gracefully
- 文件名使用 kebab-case
- 类型定义优先使用 `type` 而非 `interface`（与参考项目一致，但 Protocol 用 `interface`）
```

- [x] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md for TypeScript"
```

---

### Task 13: 编写 CLI 测试

**Files:**

- Create: `tests/cli.test.ts`

**TDD_EVIDENCE:** `bun test tests/cli.test.ts` 应通过

- [ ] **Step 1: 创建 tests/cli.test.ts**

```typescript
import { describe, it, expect, vi } from 'vitest'
import { showBanner } from '../src/cli/banner.js'
import { Spinner } from '../src/cli/spinner.js'
import { StageIndicator } from '../src/cli/stage-indicator.js'
import { createCompleter } from '../src/cli/completer.js'

describe('CLI', () => {
  describe('Banner', () => {
    it('should show banner', () => {
      const consoleSpy = vi.spyOn(console, 'log').mockImplementation(() => {})
      showBanner('gpt-4', '0.1.0', '/test')
      expect(consoleSpy).toHaveBeenCalled()
      consoleSpy.mockRestore()
    })
  })

  describe('Spinner', () => {
    it('should create spinner', () => {
      const spinner = new Spinner('Test')
      expect(spinner).toBeDefined()
    })
  })

  describe('StageIndicator', () => {
    it('should create stage indicator', () => {
      const indicator = new StageIndicator('Test')
      expect(indicator).toBeDefined()
    })
  })

  describe('Completer', () => {
    it('should complete slash commands', () => {
      const completer = createCompleter()
      const [hits, line] = completer('/he')
      expect(hits).toContain('/help')
      expect(line).toBe('/he')
    })

    it('should not complete non-slash input', () => {
      const completer = createCompleter()
      const [hits, line] = completer('hello')
      expect(hits).toHaveLength(0)
    })
  })
})
```

- [ ] **Step 2: Run test**

Run: `bun test tests/cli.test.ts`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add tests/cli.test.ts
git commit -m "test: add CLI layer tests"
```

---

### Task 14: 集成测试 + 发布

**Files:**

- None

**TDD_EVIDENCE:** `bun test` 全部通过，`bun run src/entrypoints/cli.ts` 可启动

- [ ] **Step 1: Run all tests**

Run: `bun test`
Expected: 所有测试通过

- [ ] **Step 2: Type check**

Run: `tsc --noEmit`
Expected: 无类型错误

- [ ] **Step 3: 验证 CLI 启动**

Run: `bun run src/entrypoints/cli.ts`
Expected: 显示 Banner，提示符可输入

- [ ] **Step 4: 验证 slash 命令**

在 REPL 中测试：
- `/help` - 显示帮助
- `/model` - 显示模型
- `/quit` - 退出

- [ ] **Step 5: 验证工具调用**

在 REPL 中测试：
- `Calculate 2+2` - 计算器
- `What's the weather in Beijing?` - 天气

- [ ] **Step 6: 验证 Memory**

在 REPL 中测试：
- `/soul` - 显示 soul.md
- `/user` - 显示 user.md
- `/history` - 显示历史
- `/resume` - 显示 session 列表

- [ ] **Step 7: 更新 README**

更新 README.md 中的安装和使用说明。

- [ ] **Step 8: Final commit**

```bash
git add .
git commit -m "feat: complete TypeScript migration - Phase 4"
```

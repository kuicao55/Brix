# Phase 1b — Streaming Core Implementation Plan

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** Add streaming LLM output, real-time Markdown rendering, spinner feedback, and tool execution display to Brix — transforming the CLI from "blank wait → dump" to a responsive, polished experience.

**Milestone ref:** milestone-5 from claude-progress.json

**Architecture:** Four tasks building on each other: streaming providers → streaming orchestrator → renderer/CLI integration → tool display. The streaming protocol uses `AsyncGenerator[dict, None]` with typed yield dicts (`text_delta`, `tool_call`, `tool_result`, `usage`). Rich library provides all terminal UI components via a single `Console` instance.

**Tech Stack:** Python 3.11+, Rich (Live/Markdown/Panel/Theme), OpenAI SDK streaming, Anthropic SDK streaming

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `infra/llm_client.py` | Modify | Add `chat_stream()` method |
| `infra/providers/openai_compat.py` | Modify | Add `chat_stream()` with SSE streaming |
| `infra/providers/anthropic_compat.py` | Modify | Add `chat_stream()` with Anthropic streaming |
| `orchestrator/engine.py` | Modify | Add `run_stream()` to OrchestratorEngine protocol |
| `orchestrator/state_machine.py` | Modify | Implement `run_stream()` streaming generator |
| `orchestrator/langgraph_engine.py` | Modify | Implement `run_stream()` streaming generator |
| `cli/stream_renderer.py` | Create | Safe-boundary Markdown stream renderer |
| `cli/spinner.py` | Create | Braille animation spinner |
| `cli/theme.py` | Create | Brix Rich theme |
| `cli/app.py` | Modify | Integrate streaming, spinner, Rich Console |
| `cli/display.py` | Modify | Remove passthrough, delegate to Rich |
| `cli/tool_display.py` | Create | Tool execution status panels |
| `tests/test_streaming.py` | Create | Streaming provider and renderer tests |
| `tests/test_spinner.py` | Create | Spinner lifecycle tests |
| `tests/test_tool_display.py` | Create | Tool display tests |

---

### Task 1: Streaming Providers

**Files:**

- Modify: `infra/providers/openai_compat.py`
- Modify: `infra/providers/anthropic_compat.py`
- Modify: `infra/llm_client.py`
- Test: `tests/test_streaming.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/test_streaming.py::test_openai_stream_yields_text_delta should FAIL (no chat_stream). Step 4 (GREEN): same test should PASS.`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_streaming.py — new file
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from infra.llm_client import LLMClient, LLMResponse, ToolCall


@pytest.mark.asyncio
async def test_openai_stream_yields_text_delta():
    """OpenAICompatProvider.chat_stream() should yield text_delta dicts."""
    from infra.providers.openai_compat import OpenAICompatProvider

    provider = OpenAICompatProvider()

    # Mock the OpenAI streaming response
    mock_chunks = []
    for text in ["Hello", " world", "!"]:
        chunk = MagicMock()
        chunk.choices = [MagicMock()]
        chunk.choices[0].delta.content = text
        chunk.choices[0].delta.tool_calls = None
        mock_chunks.append(chunk)

    # Final chunk with no content
    final_chunk = MagicMock()
    final_chunk.choices = [MagicMock()]
    final_chunk.choices[0].delta.content = None
    final_chunk.choices[0].delta.tool_calls = None
    mock_chunks.append(final_chunk)

    async def mock_aiter(*args, **kwargs):
        for chunk in mock_chunks:
            yield chunk

    mock_client = MagicMock()
    mock_client.chat.completions.create = AsyncMock(side_effect=mock_aiter)
    mock_client.close = AsyncMock()

    with patch("infra.providers.openai_compat.AsyncOpenAI", return_value=mock_client):
        results = []
        async for event in provider.chat_stream(
            messages=[{"role": "user", "content": "hi"}],
            model="test-model",
            tools=None,
            base_url="http://test",
            api_key="sk-test",
        ):
            results.append(event)

    text_deltas = [e for e in results if e["type"] == "text_delta"]
    assert len(text_deltas) == 3
    assert text_deltas[0]["text"] == "Hello"
    assert text_deltas[1]["text"] == " world"


@pytest.mark.asyncio
async def test_anthropic_stream_yields_text_delta():
    """AnthropicCompatProvider.chat_stream() should yield text_delta dicts."""
    from infra.providers.anthropic_compat import AnthropicCompatProvider

    provider = AnthropicCompatProvider()

    # Mock Anthropic streaming context manager
    mock_text_event = MagicMock()
    mock_text_event.type = "text"
    mock_text_event.text = "Hello world"

    mock_stream = MagicMock()
    mock_stream.__aenter__ = AsyncMock(return_value=mock_stream)
    mock_stream.__aexit__ = AsyncMock(return_value=False)
    mock_stream.text_stream = AsyncMock()

    async def mock_text_stream():
        yield "Hello"
        yield " world"

    mock_stream.text_stream = mock_text_stream()

    mock_client = MagicMock()
    mock_client.messages.stream.return_value = mock_stream
    mock_client.close = AsyncMock()

    with patch("infra.providers.anthropic_compat.AsyncAnthropic", return_value=mock_client):
        results = []
        async for event in provider.chat_stream(
            messages=[{"role": "user", "content": "hi"}],
            model="test-model",
            tools=None,
            base_url="http://test",
            api_key="sk-test",
        ):
            results.append(event)

    text_deltas = [e for e in results if e["type"] == "text_delta"]
    assert len(text_deltas) >= 1


@pytest.mark.asyncio
async def test_llm_client_chat_stream():
    """LLMClient.chat_stream() should delegate to provider.chat_stream()."""
    mock_provider = MagicMock()

    async def mock_stream(*args, **kwargs):
        yield {"type": "text_delta", "text": "hi"}
        yield {"type": "text_delta", "text": " there"}

    mock_provider.chat_stream = mock_stream

    config = {
        "providers": {
            "test": {"protocol": "openai", "base_url": "http://test", "api_key_env": "TEST_KEY"}
        }
    }
    client = LLMClient(config)

    with patch.object(client, "_get_provider", return_value=mock_provider), \
         patch.object(client, "_resolve_provider_config", return_value=(
             "openai", {"protocol": "openai", "base_url": "http://test", "api_key_env": "TEST_KEY"}, "test"
         )), \
         patch.dict("os.environ", {"TEST_KEY": "sk-test"}):

        results = []
        async for event in client.chat_stream([{"role": "user", "content": "hi"}], "test-model"):
            results.append(event)

        assert len(results) == 2
        assert results[0]["text"] == "hi"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_streaming.py -xvs`

Expected: FAIL — `chat_stream` method doesn't exist on providers or LLMClient

- [ ] **Step 3: Implement streaming providers**

Modify `infra/providers/openai_compat.py` — add `chat_stream()`:

```python
    async def chat_stream(
        self,
        messages: list[dict],
        model: str,
        tools: list[dict] | None,
        base_url: str,
        api_key: str,
    ):
        """Stream chat completion. Yields dicts with 'type' key."""
        from openai import AsyncOpenAI

        client = AsyncOpenAI(base_url=base_url, api_key=api_key)
        try:
            kwargs = {"model": model, "messages": messages, "stream": True}
            if tools:
                kwargs["tools"] = tools
                kwargs["tool_choice"] = "auto"

            stream = await client.chat.completions.create(**kwargs)

            # Accumulate tool calls across chunks
            tool_calls_acc: dict[int, dict] = {}

            async for chunk in stream:
                delta = chunk.choices[0].delta if chunk.choices else None
                if not delta:
                    continue

                if delta.content:
                    yield {"type": "text_delta", "text": delta.content}

                if delta.tool_calls:
                    for tc in delta.tool_calls:
                        idx = tc.index
                        if idx not in tool_calls_acc:
                            tool_calls_acc[idx] = {
                                "id": tc.id or "",
                                "name": "",
                                "arguments": "",
                            }
                        if tc.id:
                            tool_calls_acc[idx]["id"] = tc.id
                        if tc.function:
                            if tc.function.name:
                                tool_calls_acc[idx]["name"] = tc.function.name
                            if tc.function.arguments:
                                tool_calls_acc[idx]["arguments"] += tc.function.arguments

            # Emit accumulated tool calls
            for idx in sorted(tool_calls_acc.keys()):
                tc = tool_calls_acc[idx]
                args = tc["arguments"]
                try:
                    import json
                    parsed = json.loads(args) if isinstance(args, str) else args
                except (json.JSONDecodeError, TypeError):
                    parsed = {"raw": args}
                yield {
                    "type": "tool_call",
                    "id": tc["id"],
                    "name": tc["name"],
                    "input": parsed,
                }
        finally:
            await client.close()
```

Modify `infra/providers/anthropic_compat.py` — add `chat_stream()`:

```python
    async def chat_stream(
        self,
        messages: list[dict],
        model: str,
        tools: list[dict] | None,
        base_url: str,
        api_key: str,
    ):
        """Stream chat completion. Yields dicts with 'type' key."""
        from anthropic import AsyncAnthropic

        client = AsyncAnthropic(base_url=base_url, api_key=api_key)
        try:
            # Convert messages (reuse existing logic)
            system_msg = ""
            anthropic_messages = []
            for msg in messages:
                if msg["role"] == "system":
                    system_msg = msg["content"]
                else:
                    anthropic_messages.append(self._convert_message(msg))

            kwargs = {
                "model": model,
                "messages": anthropic_messages,
                "max_tokens": 4096,
            }
            if system_msg:
                kwargs["system"] = system_msg
            if tools:
                kwargs["tools"] = self._convert_tools(tools)

            async with client.messages.stream(**kwargs) as stream:
                async for text in stream.text_stream:
                    yield {"type": "text_delta", "text": text}

            # After stream ends, get the final message for tool calls
            final_message = await stream.get_final_message()
            for block in final_message.content:
                if block.type == "tool_use":
                    yield {
                        "type": "tool_call",
                        "id": block.id,
                        "name": block.name,
                        "input": block.input,
                    }
        finally:
            await client.close()
```

Modify `infra/llm_client.py` — add `chat_stream()`:

```python
    async def chat_stream(
        self,
        messages: list[dict],
        model: str,
        tools: list[dict] | None = None,
    ):
        """Stream chat. Yields dicts: text_delta, tool_call, usage."""
        protocol, provider_config, provider_name = self._resolve_provider_config(model)
        provider = self._get_provider(protocol)

        api_key_env = provider_config.get("api_key_env")
        if not api_key_env:
            raise ValueError(f"Provider '{provider_name}' missing 'api_key_env'")
        api_key = os.environ.get(api_key_env, "")
        if not api_key:
            raise ValueError(f"API key not found: set {api_key_env}")
        base_url = provider_config.get("base_url")
        if not base_url:
            raise ValueError(f"Provider '{provider_name}' missing 'base_url'")

        async for event in provider.chat_stream(
            messages=messages,
            model=model,
            tools=tools,
            base_url=base_url,
            api_key=api_key,
        ):
            yield event
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_streaming.py -xvs`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add infra/providers/openai_compat.py infra/providers/anthropic_compat.py infra/llm_client.py tests/test_streaming.py
git commit -m "feat: add streaming chat_stream() to OpenAI and Anthropic providers"
```

---

### Task 2: Streaming Orchestrator

**Files:**

- Modify: `orchestrator/engine.py`
- Modify: `orchestrator/state_machine.py`
- Modify: `orchestrator/langgraph_engine.py`
- Test: `tests/test_orchestrator.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/test_orchestrator.py::test_run_stream_yields_text_delta should FAIL (no run_stream). Step 4 (GREEN): same test should PASS.`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_orchestrator.py — add new tests
import pytest
from unittest.mock import AsyncMock, MagicMock
from orchestrator.engine import OrchestratorContext
from orchestrator.state_machine import StateMachineOrchestrator


@pytest.mark.asyncio
async def test_run_stream_yields_text_delta():
    """run_stream() should yield text_delta events from LLM."""
    orchestrator = StateMachineOrchestrator(max_iterations=1)

    # Mock LLM client that streams text
    mock_llm = MagicMock()

    async def mock_stream(*args, **kwargs):
        yield {"type": "text_delta", "text": "Hello"}
        yield {"type": "text_delta", "text": " world"}

    mock_llm.chat_stream = mock_stream

    context = OrchestratorContext(
        history=[],
        tool_runner=MagicMock(get_tool_schemas=MagicMock(return_value=[])),
        llm_client=mock_llm,
        model="test-model",
        hooks=None,
    )

    results = []
    async for event in orchestrator.run_stream("hi", context):
        results.append(event)

    text_events = [e for e in results if e.get("type") == "text_delta"]
    assert len(text_events) == 2
    assert text_events[0]["text"] == "Hello"


@pytest.mark.asyncio
async def test_run_stream_handles_tool_calls():
    """run_stream() should yield tool_call, execute tool, yield tool_result."""
    orchestrator = StateMachineOrchestrator(max_iterations=2)

    call_count = 0

    async def mock_stream_first(*args, **kwargs):
        yield {"type": "text_delta", "text": "Let me calculate"}
        yield {"type": "tool_call", "id": "call_1", "name": "calculator", "input": {"expr": "2+2"}}

    async def mock_stream_second(*args, **kwargs):
        yield {"type": "text_delta", "text": "The result is 4"}

    streams = [mock_stream_first(), mock_stream_second()]

    mock_llm = MagicMock()

    async def mock_chat_stream(*args, **kwargs):
        nonlocal call_count
        idx = min(call_count, len(streams) - 1)
        call_count += 1
        async for event in streams[idx]:
            yield event

    mock_llm.chat_stream = mock_chat_stream

    mock_runner = MagicMock()
    mock_runner.get_tool_schemas = MagicMock(return_value=[])
    mock_runner.run = AsyncMock(return_value="4")

    context = OrchestratorContext(
        history=[],
        tool_runner=mock_runner,
        llm_client=mock_llm,
        model="test-model",
        hooks=None,
    )

    results = []
    async for event in orchestrator.run_stream("what is 2+2", context):
        results.append(event)

    types = [e["type"] for e in results]
    assert "tool_call" in types
    assert "tool_result" in types
    assert "text_delta" in types
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_orchestrator.py::test_run_stream_yields_text_delta tests/test_orchestrator.py::test_run_stream_handles_tool_calls -xvs`

Expected: FAIL — `run_stream` method doesn't exist

- [ ] **Step 3: Implement run_stream on orchestrators**

Modify `orchestrator/engine.py` — add to protocol:

```python
from typing import AsyncGenerator

@runtime_checkable
class OrchestratorEngine(Protocol):
    """Protocol that all orchestrator implementations must satisfy."""

    async def run(self, user_input: str, context: OrchestratorContext) -> str:
        ...

    async def run_stream(self, user_input: str, context: OrchestratorContext) -> AsyncGenerator[dict, None]:
        ...
```

Modify `orchestrator/state_machine.py` — add `run_stream()`:

```python
    async def run_stream(self, user_input: str, context: OrchestratorContext):
        """Streaming orchestrator loop. Yields typed event dicts."""
        context.history.append({"role": "user", "content": user_input})

        for self._current_iter in range(1, self.max_iterations + 1):
            # Stream the LLM response
            full_content = ""
            tool_calls = []

            tool_schemas = []
            if context.tool_runner and hasattr(context.tool_runner, "get_tool_schemas"):
                tool_schemas = context.tool_runner.get_tool_schemas()

            async for event in context.llm_client.chat_stream(
                messages=context.history,
                model=context.model,
                tools=tool_schemas if tool_schemas else None,
            ):
                if event["type"] == "text_delta":
                    full_content += event["text"]
                    yield event
                elif event["type"] == "tool_call":
                    tool_calls.append(event)
                    yield event

            if not tool_calls:
                # No tool calls — done
                context.history.append({"role": "assistant", "content": full_content})
                return

            # Execute tool calls
            tc_records = [
                {
                    "id": tc.get("id") or f"call_{uuid.uuid4().hex[:12]}",
                    "name": tc["name"],
                    "arguments": tc["input"],
                }
                for tc in tool_calls
            ]
            context.history.append({
                "role": "assistant",
                "content": full_content,
                "tool_calls": tc_records,
            })

            for tc in tc_records:
                t0 = time.monotonic()
                try:
                    result = await context.tool_runner.run(tc["name"], tc["arguments"])
                except Exception as e:
                    result = f"Error executing {tc['name']}: {e}"
                elapsed = int((time.monotonic() - t0) * 1000)

                yield {
                    "type": "tool_result",
                    "id": tc["id"],
                    "name": tc["name"],
                    "result": str(result),
                    "ms": elapsed,
                }

                if context.hooks:
                    context.hooks.fire(
                        "tool_exec",
                        name=tc["name"],
                        args=tc["arguments"],
                        result=str(result)[:100],
                        ms=elapsed,
                    )

                context.history.append({
                    "role": "tool",
                    "tool_call_id": tc["id"],
                    "tool_name": tc["name"],
                    "content": str(result),
                })

        # Exhausted iterations
        fallback = "I was unable to complete the request within the allowed steps."
        context.history.append({"role": "assistant", "content": fallback})
        yield {"type": "text_delta", "text": fallback}
```

Modify `orchestrator/langgraph_engine.py` — add `run_stream()`:

```python
    async def run_stream(self, user_input: str, context: OrchestratorContext):
        """Streaming orchestrator. For LangGraph, delegates to state machine style loop."""
        # LangGraph's graph structure doesn't map cleanly to streaming.
        # Use a simple loop similar to StateMachineOrchestrator.
        context.history.append({"role": "user", "content": user_input})

        for iteration in range(1, self.max_iterations + 1):
            full_content = ""
            tool_calls = []

            tool_schemas = []
            if context.tool_runner and hasattr(context.tool_runner, "get_tool_schemas"):
                tool_schemas = context.tool_runner.get_tool_schemas()

            async for event in context.llm_client.chat_stream(
                messages=context.history,
                model=context.model,
                tools=tool_schemas if tool_schemas else None,
            ):
                if event["type"] == "text_delta":
                    full_content += event["text"]
                    yield event
                elif event["type"] == "tool_call":
                    tool_calls.append(event)
                    yield event

            if not tool_calls:
                context.history.append({"role": "assistant", "content": full_content})
                return

            tc_records = [
                {
                    "id": tc.get("id") or f"call_{uuid.uuid4().hex[:12]}",
                    "name": tc["name"],
                    "arguments": tc["input"],
                }
                for tc in tool_calls
            ]
            context.history.append({
                "role": "assistant",
                "content": full_content,
                "tool_calls": tc_records,
            })

            for tc in tc_records:
                t0 = time.monotonic()
                try:
                    result = await context.tool_runner.run(tc["name"], tc["arguments"])
                except Exception as e:
                    result = f"Error executing {tc['name']}: {e}"
                elapsed = int((time.monotonic() - t0) * 1000)

                yield {
                    "type": "tool_result",
                    "id": tc["id"],
                    "name": tc["name"],
                    "result": str(result),
                    "ms": elapsed,
                }

                if context.hooks:
                    context.hooks.fire(
                        "tool_exec",
                        name=tc["name"],
                        args=tc["arguments"],
                        result=str(result)[:100],
                        ms=elapsed,
                    )

                context.history.append({
                    "role": "tool",
                    "tool_call_id": tc["id"],
                    "tool_name": tc["name"],
                    "content": str(result),
                })

        fallback = "I was unable to complete the request within the allowed steps."
        context.history.append({"role": "assistant", "content": fallback})
        yield {"type": "text_delta", "text": fallback}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_orchestrator.py::test_run_stream_yields_text_delta tests/test_orchestrator.py::test_run_stream_handles_tool_calls -xvs`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add orchestrator/engine.py orchestrator/state_machine.py orchestrator/langgraph_engine.py tests/test_orchestrator.py
git commit -m "feat: add run_stream() streaming protocol to orchestrators"
```

---

### Task 3: StreamRenderer + Spinner + Theme + CLI Integration

**Files:**

- Create: `cli/stream_renderer.py`
- Create: `cli/spinner.py`
- Create: `cli/theme.py`
- Modify: `cli/app.py`
- Modify: `cli/display.py`
- Test: `tests/test_cli.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/test_cli.py::test_stream_renderer_safe_boundary should FAIL (no StreamRenderer). Step 4 (GREEN): same test should PASS.`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_cli.py — add new tests
import pytest
from cli.stream_renderer import StreamRenderer
from cli.spinner import Spinner
from cli.theme import BRIX_THEME


def test_stream_renderer_safe_boundary():
    """StreamRenderer should only render at safe Markdown boundaries."""
    from rich.console import Console
    import io

    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    renderer = StreamRenderer(console)

    # Push incomplete code fence — should NOT render yet
    renderer.push_delta("```python\nprint('hello')")
    assert renderer.rendered == ""  # Not rendered yet — fence not closed

    # Close the fence — NOW should render
    renderer.push_delta("\n```\n")
    assert "```python" in renderer.rendered
    assert "```" in renderer.rendered


def test_stream_renderer_flush():
    """flush() should render all remaining buffered content."""
    from rich.console import Console
    import io

    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    renderer = StreamRenderer(console)

    renderer.push_delta("Hello world")
    assert renderer.rendered == ""  # No safe boundary yet

    renderer.flush()
    assert "Hello world" in renderer.rendered


def test_spinner_lifecycle():
    """Spinner should start, update, and finish without error."""
    from rich.console import Console
    import io

    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)

    spinner = Spinner(console, label="Testing...")
    spinner.start()
    spinner.update_label("Still testing...")
    spinner.finish("Done")
    # Should not raise


def test_theme_has_required_styles():
    """BRIX_THEME should define all required style keys."""
    from rich.theme import Theme
    assert isinstance(BRIX_THEME, Theme)
    # Check key styles exist
    for key in ["markdown.h1", "markdown.code_block", "tool.name", "spinner.active"]:
        # Theme.get() returns None for missing keys
        style = BRIX_THEME.styles.get(key)
        assert style is not None, f"Missing theme style: {key}"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_cli.py::test_stream_renderer_safe_boundary tests/test_cli.py::test_stream_renderer_flush tests/test_cli.py::test_spinner_lifecycle tests/test_cli.py::test_theme_has_required_styles -xvs`

Expected: FAIL — modules don't exist

- [ ] **Step 3: Implement StreamRenderer, Spinner, Theme**

Create `cli/theme.py`:

```python
"""Brix terminal theme for Rich."""

from rich.style import Style
from rich.theme import Theme

BRIX_THEME = Theme(
    {
        "markdown.h1": Style(bold=True, color="cyan"),
        "markdown.h2": Style(bold=True, color="bright_white"),
        "markdown.h3": Style(color="blue"),
        "markdown.code": Style(color="green"),
        "markdown.code_block": Style(bgcolor="grey11"),
        "markdown.link": Style(color="blue", underline=True),
        "markdown.em": Style(italic=True, color="magenta"),
        "markdown.strong": Style(bold=True, color="yellow"),
        "markdown.blockquote": Style(color="grey50"),
        "tool.border": Style(color="grey50"),
        "tool.name": Style(bold=True, color="cyan"),
        "tool.success": Style(color="green"),
        "tool.error": Style(color="red"),
        "spinner.active": Style(color="blue"),
        "spinner.done": Style(color="green"),
        "spinner.failed": Style(color="red"),
    }
)
```

Create `cli/spinner.py`:

```python
"""Braille animation spinner with Rich Live display."""

from __future__ import annotations

import threading
import time

from rich.console import Console
from rich.live import Live
from rich.text import Text

BRAILLE_FRAMES = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"]


class Spinner:
    """Animated spinner with elapsed time display."""

    def __init__(self, console: Console, label: str = "Thinking...") -> None:
        self.console = console
        self.label = label
        self.frame_idx = 0
        self.start_time = 0.0
        self.running = False
        self.live: Live | None = None
        self._thread: threading.Thread | None = None

    def _render_frame(self) -> Text:
        elapsed = time.time() - self.start_time
        frame = BRAILLE_FRAMES[self.frame_idx % len(BRAILLE_FRAMES)]
        text = Text()
        text.append(f"  {frame} ", style="spinner.active")
        text.append(self.label, style="dim")
        text.append(f"  {elapsed:.1f}s", style="dim cyan")
        return text

    def start(self) -> None:
        self.running = True
        self.start_time = time.time()
        self.live = Live(
            self._render_frame(),
            console=self.console,
            refresh_per_second=10,
            transient=True,
        )
        self.live.start()
        self._thread = threading.Thread(target=self._animate, daemon=True)
        self._thread.start()

    def _animate(self) -> None:
        while self.running:
            self.frame_idx += 1
            if self.live:
                self.live.update(self._render_frame())
            time.sleep(0.1)

    def update_label(self, label: str) -> None:
        self.label = label

    def finish(self, label: str = "Done") -> None:
        self.running = False
        if self._thread:
            self._thread.join(timeout=0.5)
        if self.live:
            self.live.stop()
        elapsed = time.time() - self.start_time
        self.console.print(f"  [green]✓[/] {label}  [dim]{elapsed:.1f}s[/]")

    def fail(self, label: str = "Failed") -> None:
        self.running = False
        if self._thread:
            self._thread.join(timeout=0.5)
        if self.live:
            self.live.stop()
        elapsed = time.time() - self.start_time
        self.console.print(f"  [red]✗[/] {label}  [dim]{elapsed:.1f}s[/]")
```

Create `cli/stream_renderer.py`:

```python
"""Safe-boundary Markdown stream renderer using Rich Live."""

from __future__ import annotations

from rich.console import Console
from rich.live import Live
from rich.markdown import Markdown


class StreamRenderer:
    """Buffers streaming text and renders at safe Markdown boundaries.

    Inspired by Claw Code's MarkdownStreamState: waits for complete code
    fences or empty lines before rendering to prevent broken Markdown display.
    """

    def __init__(self, console: Console) -> None:
        self.console = console
        self.pending = ""
        self.rendered = ""
        self.live: Live | None = None

    def start(self) -> None:
        self.live = Live(
            console=self.console,
            refresh_per_second=15,
            transient=False,  # Keep rendered output visible
        )
        self.live.start()

    def push_delta(self, delta: str) -> None:
        """Receive a text chunk. Renders at safe boundaries."""
        self.pending += delta
        boundary = self._find_safe_boundary(self.pending)
        if boundary is not None:
            ready = self.pending[:boundary]
            self.pending = self.pending[boundary:]
            self.rendered += ready
            self._update_display()

    def flush(self) -> None:
        """Render all remaining buffered content and stop Live display."""
        if self.pending:
            self.rendered += self.pending
            self.pending = ""
            self._update_display()
        if self.live:
            self.live.stop()
            self.live = None

    def _find_safe_boundary(self, text: str) -> int | None:
        """Find the last safe render point in text.

        Safe points: after a closed code fence (```), or after an empty line
        when not inside a code fence.
        """
        lines = text.split("\n")
        in_fence = False
        last_safe = 0

        for i, line in enumerate(lines):
            stripped = line.strip()
            if stripped.startswith("```"):
                in_fence = not in_fence
                if not in_fence:
                    # Code fence just closed — safe point
                    pos = sum(len(l) + 1 for l in lines[: i + 1])
                    last_safe = pos
            elif not in_fence and stripped == "" and i > 0:
                # Empty line outside code fence — safe point
                pos = sum(len(l) + 1 for l in lines[: i + 1])
                last_safe = pos

        return last_safe if last_safe > 0 else None

    def _update_display(self) -> None:
        if self.live and self.rendered:
            md = Markdown(self.rendered)
            self.live.update(md)
```

Modify `cli/display.py`:

```python
"""Display utilities for Brix CLI."""

from __future__ import annotations


def format_response(text: str) -> str:
    """Format an assistant response for terminal output.

    When Rich streaming is active, this is a passthrough.
    Legacy fallback for non-streaming paths.
    """
    return text
```

Modify `cli/app.py` — integrate streaming, spinner, Rich Console:

```python
# Add imports at top:
from pathlib import Path
from rich.console import Console
from cli.banner import show_banner
from cli.spinner import Spinner
from cli.stream_renderer import StreamRenderer
from cli.theme import BRIX_THEME

# In BrixCLI.__init__, add:
    self._console = Console(theme=BRIX_THEME)

# In BrixCLI.run(), replace the banner and REPL loop:
    async def run(self) -> None:
        """Start the REPL loop."""
        session: PromptSession[str] = PromptSession(history=InMemoryHistory())
        default_model = self._config.get("routing", {}).get("default_model", "unknown")
        show_banner(model=default_model, version="0.1.0", cwd=str(Path.cwd()))

        while True:
            try:
                user_input = await session.prompt_async("you> ")
            except (EOFError, KeyboardInterrupt):
                self._console.print("\n[dim]Goodbye.[/]")
                break

            text = user_input.strip()
            if not text:
                continue

            if text.startswith("/"):
                if self._handle_command(text):
                    continue

            try:
                response = await self._process_streaming(text)
            except Exception as exc:
                response = f"Error: {exc}"
                self._console.print(f"[red]Error:[/] {exc}")

            # Fallback: if _process_streaming returned a string (error path)
            if isinstance(response, str):
                self._console.print(format_response(response))

# Add new streaming process method:
    async def _process_streaming(self, user_input: str) -> str:
        """Process user input with streaming output."""
        log = FlowLog(user_input)
        hooks = HookRegistry()
        hooks.bind_log(log)

        history = self._memory.get_history()
        context_window = self._strategy.get_context_window(history)
        hooks.fire("memory", msgs=len(history),
                   window=len(context_window),
                   chars=sum(len(m.get("content", "")) for m in context_window),
                   context_window=[{"role": m.get("role"), "content": m.get("content", "")}
                                   for m in context_window])

        default_model = self._config.get("routing", {}).get("default_model", "")
        intent = await classify_intent(
            user_input, context_window, self._llm_client,
            default_model, hooks=hooks,
        )
        complexity = evaluate_complexity(user_input)
        model = select_model(intent, complexity, self._config)

        hooks.fire("complexity", result=complexity)
        hooks.fire("router", model=model, reason=f"{intent}->{complexity}")
        log.set_model(model)

        context = OrchestratorContext(
            history=list(context_window),
            tool_runner=self._tool_runner,
            llm_client=self._llm_client,
            model=model,
            hooks=hooks,
        )

        # Spinner while waiting for first token
        spinner = Spinner(self._console, label="Thinking...")
        spinner.start()

        full_response = ""
        renderer: StreamRenderer | None = None

        try:
            async for event in self._orchestrator.run_stream(user_input, context):
                if event["type"] == "text_delta":
                    if renderer is None:
                        # First token — stop spinner, start renderer
                        spinner.finish("Response")
                        renderer = StreamRenderer(self._console)
                        renderer.start()
                    renderer.push_delta(event["text"])
                    full_response += event["text"]
                elif event["type"] == "tool_call":
                    if renderer:
                        renderer.flush()
                    self._console.print(f"\n  [cyan]⚡ {event['name']}[/]")
                elif event["type"] == "tool_result":
                    status = "[green]✓[/]" if "Error" not in event["result"] else "[red]✗[/]"
                    self._console.print(f"  {status} {event['name']}  [dim]{event.get('ms', 0)}ms[/]")

        except Exception as exc:
            if renderer:
                renderer.flush()
            spinner.fail(str(exc))
            log.set_error(str(exc))
            try:
                flush_log(log)
            except Exception:
                pass
            raise

        if renderer:
            renderer.flush()
        elif spinner.running:
            spinner.finish("Done")

        if not full_response:
            full_response = "(no response)"

        # Persist
        is_error = full_response.startswith("Error")
        saved_count = 0
        if self._strategy.should_save({"role": "user", "content": user_input}):
            self._memory.add_message("user", user_input)
            saved_count += 1
        if not is_error and self._strategy.should_save({"role": "assistant", "content": full_response}):
            self._memory.add_message("assistant", full_response)
            saved_count += 1
        self._memory.save()
        hooks.fire("persist", saved=saved_count)

        try:
            flush_log(log)
        except Exception:
            pass

        return full_response
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_cli.py -xvs`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/stream_renderer.py cli/spinner.py cli/theme.py cli/display.py cli/app.py tests/test_cli.py
git commit -m "feat: add StreamRenderer, Spinner, Theme, and streaming CLI integration"
```

---

### Task 4: Tool Display Panel

**Files:**

- Create: `cli/tool_display.py`
- Modify: `cli/app.py`
- Test: `tests/test_tool_display.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/test_tool_display.py::test_tool_display_formats_bash should FAIL (no ToolDisplay). Step 4 (GREEN): same test should PASS.`

- [ ] **Step 1: Write the failing tests**

```python
# tests/test_tool_display.py — new file
import pytest
import io
from rich.console import Console
from cli.tool_display import ToolDisplay


def test_tool_display_formats_bash():
    """ToolDisplay should format bash commands with $ prefix."""
    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    display = ToolDisplay(console)

    display.show_tool_start("bash", {"command": "ls -la"})
    output = buf.getvalue()
    assert "ls -la" in output
    assert "$" in output


def test_tool_display_formats_file_read():
    """ToolDisplay should format file_read with path."""
    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    display = ToolDisplay(console)

    display.show_tool_start("file_read", {"path": "/tmp/test.txt"})
    output = buf.getvalue()
    assert "/tmp/test.txt" in output


def test_tool_display_result_success():
    """show_tool_result should show success indicator."""
    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    display = ToolDisplay(console)

    display.show_tool_result("bash", "output text", elapsed_ms=150.0)
    output = buf.getvalue()
    assert "bash" in output
    assert "150ms" in output


def test_tool_display_result_error():
    """show_tool_result with is_error should show error indicator."""
    buf = io.StringIO()
    console = Console(file=buf, force_terminal=True, width=80)
    display = ToolDisplay(console)

    display.show_tool_result("bash", "command not found", elapsed_ms=50.0, is_error=True)
    output = buf.getvalue()
    assert "command not found" in output
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_tool_display.py -xvs`

Expected: FAIL — `ToolDisplay` doesn't exist

- [ ] **Step 3: Implement ToolDisplay**

Create `cli/tool_display.py`:

```python
"""Tool execution status display panels."""

from __future__ import annotations

import json

from rich.console import Console
from rich.panel import Panel
from rich.text import Text


class ToolDisplay:
    """Display tool execution details with formatted panels."""

    TOOL_ICONS = {
        "bash": "⚡",
        "file_read": "📄",
        "file_write": "✏️",
        "file_edit": "📝",
        "web_search": "🔎",
        "calculator": "🧮",
        "weather": "🌤",
    }

    def __init__(self, console: Console) -> None:
        self.console = console

    def show_tool_start(self, tool_name: str, tool_input: dict) -> None:
        """Display a panel when a tool starts executing."""
        icon = self.TOOL_ICONS.get(tool_name, "🔧")
        detail = self._format_detail(tool_name, tool_input)

        panel = Panel(
            detail,
            title=f"[tool.name]{icon} {tool_name}[/]",
            title_align="left",
            border_style="tool.border",
            padding=(0, 1),
        )
        self.console.print(panel)

    def show_tool_result(
        self,
        tool_name: str,
        result: str,
        elapsed_ms: float,
        is_error: bool = False,
    ) -> None:
        """Display a one-line tool result summary."""
        icon = self.TOOL_ICONS.get(tool_name, "🔧")
        status_style = "tool.error" if is_error else "tool.success"
        status_icon = "✗" if is_error else "✓"
        elapsed_str = f"{elapsed_ms:.0f}ms"

        preview = result[:200].replace("\n", " ")
        if len(result) > 200:
            preview += "…"

        text = Text()
        text.append(f"  {icon} ", style="dim")
        text.append(f"{tool_name}", style="tool.name")
        text.append(f"  {status_icon}", style=status_style)
        text.append(f"  {elapsed_str}", style="dim cyan")
        if is_error:
            text.append(f"  {preview}", style="tool.error")

        self.console.print(text)

    def _format_detail(self, tool_name: str, tool_input: dict) -> str:
        """Format tool-specific input details."""
        if tool_name == "bash":
            cmd = tool_input.get("command", "")
            return f"[white on grey11]$ {cmd}[/]"
        elif tool_name == "file_read":
            path = tool_input.get("path", "")
            return f"📄 Reading [link]{path}[/]"
        elif tool_name == "file_write":
            path = tool_input.get("path", "")
            content = tool_input.get("content", "")
            lines = content.count("\n") + 1
            return f"✏️ Writing [link]{path}[/] ({lines} lines)"
        elif tool_name == "file_edit":
            path = tool_input.get("path", "")
            return f"📝 Editing [link]{path}[/]"
        elif tool_name == "web_search":
            query = tool_input.get("query", "")
            return f"🔎 Searching: {query}"
        else:
            preview = json.dumps(tool_input, ensure_ascii=False)[:150]
            return preview
```

Integrate into `cli/app.py` — add tool_display to the streaming process:

```python
# Add import:
from cli.tool_display import ToolDisplay

# In _process_streaming, after hooks setup:
    tool_display = ToolDisplay(self._console)

# Replace the tool_call/tool_result console.print lines:
                elif event["type"] == "tool_call":
                    if renderer:
                        renderer.flush()
                    tool_display.show_tool_start(event["name"], event.get("input", {}))
                elif event["type"] == "tool_result":
                    is_err = "Error" in event.get("result", "")
                    tool_display.show_tool_result(
                        event["name"],
                        event.get("result", ""),
                        event.get("ms", 0),
                        is_error=is_err,
                    )
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/test_tool_display.py -xvs`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/tool_display.py cli/app.py tests/test_tool_display.py
git commit -m "feat: add ToolDisplay panel with hook-integrated tool status display"
```

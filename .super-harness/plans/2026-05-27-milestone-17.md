# CLI 集成 + /model 命令 + 清理 Implementation Plan

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** 将 side 层接入主流程，改造 /model 命令支持切换和持久化，删除旧的 router/ 层。

**Milestone ref:** milestone-17 from claude-progress.json

**Architecture:** 修改 cli/app.py 集成 SideTaskManager，删除 Intent/Complexity/Route 阶段，接入 side tasks。改造 ModelCommand 支持运行时切换和持久化。最后删除 router/ 目录。

**Tech Stack:** Python 3.11+, pytest, yaml

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `cli/app.py` | Modify | 集成 SideTaskManager，删除 intent/complexity/route |
| `capability/command/builtin/info.py` | Modify | 改造 ModelCommand 支持切换和持久化 |
| `capability/basics/commands.py` | Modify | 更新 /model 描述 |
| `router/intent.py` | Delete | 被 side 层替代 |
| `router/complexity.py` | Delete | 不再需要 |
| `router/model_router.py` | Delete | 不再需要 |
| `router/__init__.py` | Delete | 包不再需要 |
| `tests/test_router.py` | Delete | 测试的是被删除的模块 |
| `tests/side/test_cli_integration.py` | Create | CLI 集成测试 |

---

### Task 1: CLI 集成 — 初始化 SideTaskManager + 修改 _process_streaming

**Files:**

- Modify: `cli/app.py`
- Test: `tests/side/test_cli_integration.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/side/test_cli_integration.py should FAIL. Step 4 (GREEN): same command should PASS.`

- [ ] **Step 1: Write the failing test**

```python
# tests/side/test_cli_integration.py
"""CLI 集成 side 层测试。"""

from __future__ import annotations

import pytest
from unittest.mock import MagicMock, patch


def test_cli_imports_side_manager():
    """cli/app.py 导入 SideTaskManager。"""
    import cli.app
    # 验证模块可以导入（不抛出异常）
    assert hasattr(cli.app, 'BrixCLI')


def test_config_routing_no_intent_model():
    """config 中不再有 intent_model 和 chat_model。"""
    from config.loader import load_config
    config = load_config()
    routing = config.get("routing", {})
    assert "intent_model" not in routing
    assert "chat_model" not in routing
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/side/test_cli_integration.py::test_config_routing_no_intent_model -v`
Expected: FAIL with `AssertionError: 'intent_model' not in routing`（因为 config 还没改）

- [ ] **Step 3: Write minimal implementation**

修改 `cli/app.py`：

1. 删除 import：
```python
# 删除以下 import
from router.intent import classify_intent
from router.complexity import evaluate_complexity
from router.model_router import select_model
```

2. 添加 import：
```python
from side.manager import SideTaskManager
from side.tasks import ALL_TASKS
```

3. 在 `__init__()` 中初始化 SideTaskManager（在 `_register_commands()` 之后）：
```python
self._side_manager = SideTaskManager()
self._side_manager.configure(
    config=self._config,
    llm_client=self._llm_client,
    memory=self._memory,
)
for task in ALL_TASKS:
    self._side_manager.register(task)
```

4. 修改 `_process_streaming()`：
   - 删除 Intent stage（行 324-335）
   - 删除 Complexity + Route stages（行 337-346）
   - 替换为：
```python
# Side 层：历史搜索（如果触发）
if self._side_manager and self._side_manager.enabled:
    indicator.update("Side", "history_search")
    search_results = await self._side_manager.run_task(
        "history_search",
        session_messages=context_messages,
        user_input=user_input,
        hooks=hooks,
    )
    if search_results:
        search_summary = "\n".join(
            f"- {s.get('title', '无标题')}: {s.get('summary', '')[:100]}"
            for s in search_results
        )
        context_messages.append({
            "role": "user",
            "content": f"[系统] 以下是你之前的对话，可能与当前问题相关：\n{search_summary}",
        })
    _tick("side:history_search")

# 直接使用 config 中的主模型
model = self._config.get("routing", {}).get("default_model", "")
hooks.fire("router", model=model, reason="direct_config")
log.set_model(model)

# 用户消息计数（用于 pref_detection 间隔）
if self._side_manager:
    self._side_manager.on_user_message()
```

5. 在 tool_result 事件后添加摘要（行 416-426 之后）：
```python
# fire-and-forget 工具摘要
if self._side_manager and self._side_manager.enabled:
    self._side_manager.fire_and_forget(
        "tool_summary",
        session_messages=context_messages,
        user_input=user_input,
        hooks=hooks,
    )
```

6. 在持久化后添加偏好检测（行 478-484 之后）：
```python
# 偏好检测（按间隔触发）
if (self._side_manager and self._side_manager.enabled
        and self._side_manager.should_run_pref_detection()):
    self._side_manager.fire_and_forget(
        "pref_detection",
        session_messages=context_messages,
        user_input=user_input,
        hooks=hooks,
    )
```

7. 修改语音清理（行 522-531）：
```python
# 改前
cleanup_model = self._config.get("routing", {}).get(
    "intent_model", "ali/qwen3.6-flash"
)

async def _cleanup_llm(prompt: str) -> str:
    resp = await self._llm_client.chat(
        messages=[{"role": "user", "content": prompt}],
        model=cleanup_model,
    )
    return resp.content

# 改后
side_model = self._side_manager.get_side_model() if self._side_manager else "ali/qwen3.6-flash"

async def _cleanup_llm(prompt: str) -> str:
    resp = await self._llm_client.chat(
        messages=[{"role": "user", "content": prompt}],
        model=side_model,
    )
    return resp.content
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/side/test_cli_integration.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add cli/app.py tests/side/test_cli_integration.py
git commit -m "feat(cli): 集成 SideTaskManager，删除 intent/complexity/route 阶段"
```

---

### Task 2: /model 命令改造

**Files:**

- Modify: `capability/command/builtin/info.py`
- Modify: `capability/basics/commands.py`

**TDD_EVIDENCE:** `Step 2 (RED): pytest tests/side/test_cli_integration.py::test_model_command_switch -v should FAIL. Step 4 (GREEN): same command should PASS.`

- [ ] **Step 1: Write the failing test**

```python
# 在 tests/side/test_cli_integration.py 中追加

def test_model_command_displays_models():
    """/model 无参数时显示当前模型和可用模型列表。"""
    from capability.command.builtin.info import ModelCommand
    from capability.base import CommandContext

    config = {
        "routing": {"default_model": "minimax/MiniMax-M2.7"},
        "models": [
            {"id": "minimax/MiniMax-M2.7", "cost_tier": "high"},
            {"id": "ali/qwen3.6-flash", "cost_tier": "low"},
        ],
    }
    cmd = ModelCommand(config)
    ctx = CommandContext(memory=None, hooks=None)

    import io
    import sys
    old_stdout = sys.stdout
    sys.stdout = io.StringIO()
    import asyncio
    asyncio.run(cmd.execute("", ctx))
    output = sys.stdout.getvalue()
    sys.stdout = old_stdout

    assert "minimax/MiniMax-M2.7" in output
    assert "ali/qwen3.6-flash" in output


def test_model_command_switch():
    """/model <id> 切换模型。"""
    from capability.command.builtin.info import ModelCommand
    from capability.base import CommandContext

    config = {
        "routing": {"default_model": "minimax/MiniMax-M2.7"},
        "models": [
            {"id": "minimax/MiniMax-M2.7", "cost_tier": "high"},
            {"id": "ali/qwen3.6-flash", "cost_tier": "low"},
        ],
    }
    cmd = ModelCommand(config)
    ctx = CommandContext(memory=None, hooks=None)

    import io
    import sys
    old_stdout = sys.stdout
    sys.stdout = io.StringIO()
    import asyncio
    asyncio.run(cmd.execute("ali/qwen3.6-flash", ctx))
    output = sys.stdout.getvalue()
    sys.stdout = old_stdout

    assert "已切换" in output
    assert config["routing"]["default_model"] == "ali/qwen3.6-flash"


def test_model_command_invalid():
    """/model <invalid_id> 提示未知模型。"""
    from capability.command.builtin.info import ModelCommand
    from capability.base import CommandContext

    config = {
        "routing": {"default_model": "minimax/MiniMax-M2.7"},
        "models": [{"id": "minimax/MiniMax-M2.7"}],
    }
    cmd = ModelCommand(config)
    ctx = CommandContext(memory=None, hooks=None)

    import io
    import sys
    old_stdout = sys.stdout
    sys.stdout = io.StringIO()
    import asyncio
    asyncio.run(cmd.execute("nonexistent/model", ctx))
    output = sys.stdout.getvalue()
    sys.stdout = old_stdout

    assert "未知模型" in output
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/side/test_cli_integration.py::test_model_command_switch -v`
Expected: FAIL（当前 ModelCommand 不支持切换）

- [ ] **Step 3: Write minimal implementation**

修改 `capability/command/builtin/info.py`：

```python
class ModelCommand(Command):
    """查看或切换主模型。"""

    def __init__(self, config: dict) -> None:
        self._config = config

    @property
    def meta(self) -> CommandMeta:
        return CommandMeta(
            name="model",
            description="查看或切换主模型 (/model [model_id])",
            type=CommandType.SYSTEM,
        )

    async def execute(self, args: str, context: CommandContext) -> CommandResult:
        # 无参数：显示当前模型和可用模型列表
        if not args.strip():
            default_model = self._config.get("routing", {}).get("default_model", "unknown")
            print(f"\n  当前主模型: {default_model}")
            print(f"\n  可用模型:")
            for model in self._config.get("models", []):
                model_id = model.get("id", "")
                cost = model.get("cost_tier", "?")
                marker = " *" if model_id == default_model else ""
                print(f"    {model_id} [{cost}]{marker}")
            print("\n  使用 /model <model_id> 切换模型\n")
            return CommandResult(type=CommandResultType.NONE)

        # 有参数：切换模型
        model_id = args.strip()
        models = self._config.get("models", [])
        valid_ids = {m.get("id") for m in models}
        if model_id not in valid_ids:
            print(f"\n  未知模型: {model_id}")
            print(f"  使用 /model 查看可用模型列表\n")
            return CommandResult(type=CommandResultType.NONE)

        # 运行时切换（不持久化）
        self._config.setdefault("routing", {})["default_model"] = model_id
        print(f"\n  已切换到: {model_id}\n")
        return CommandResult(type=CommandResultType.NONE)
```

修改 `capability/basics/commands.py`：

```python
# 改前
("/model",       "查看当前默认模型"),

# 改后
("/model",       "查看或切换主模型 (/model [model_id])"),
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/side/test_cli_integration.py -k "model" -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add capability/command/builtin/info.py capability/basics/commands.py tests/side/test_cli_integration.py
git commit -m "feat(command): /model 命令支持切换主模型"
```

---

### Task 3: 删除 router/ + 回归测试

**Files:**

- Delete: `router/intent.py`
- Delete: `router/complexity.py`
- Delete: `router/model_router.py`
- Delete: `router/__init__.py`
- Delete: `tests/test_router.py`

**TDD_EVIDENCE:** `验证删除后无 import 错误，所有测试通过。`

- [ ] **Step 1: Write the failing test**

```python
# 在 tests/side/test_cli_integration.py 中追加

def test_router_deleted():
    """router/ 目录已被删除。"""
    import os
    assert not os.path.exists("router"), "router/ directory should be deleted"
    assert not os.path.exists("tests/test_router.py"), "tests/test_router.py should be deleted"


def test_no_router_imports():
    """cli/app.py 中不再 import router 模块。"""
    with open("cli/app.py") as f:
        content = f.read()
    assert "from router" not in content
    assert "import router" not in content
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/side/test_cli_integration.py::test_router_deleted -v`
Expected: FAIL（router/ 目录还存在）

- [ ] **Step 3: Write minimal implementation**

```bash
rm -rf router/
rm -f tests/test_router.py
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && .venv/bin/python -m pytest tests/ -v`
Expected: PASS（所有测试通过，无 import 错误）

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: 删除 router/ 目录和相关测试"
```

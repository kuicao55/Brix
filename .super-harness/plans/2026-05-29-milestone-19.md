# 搜索工具 + 记忆注入 Implementation Plan

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** 实现记忆搜索工具和短期记忆注入机制，让主模型可按需检索长期记忆，短期记忆自动注入对话上下文

**Milestone ref:** milestone-19 from claude-progress.json

**Architecture:** 实现 KeywordMemorySearcher（关键词搜索）+ MemorySearchTool（注册到 ToolRunner），以及 MemorySummaryTask（短期记忆总结注入）。改造 BrixMemoryProvider 集成 ShortTermMemory 和 LongTermMemory。

**Tech Stack:** Python 3.11+, pytest, tiktoken

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `memory/searcher.py` | Create | KeywordMemorySearcher + MemorySearcher Protocol |
| `capability/tools/memory_search.py` | Create | MemorySearchTool |
| `side/tasks/memory_summary.py` | Create | MemorySummaryTask |
| `memory/provider.py` | Modify | 集成 ShortTermMemory + LongTermMemory |
| `cli/app.py` | Modify | 注册 MemorySearchTool + MemorySummaryTask |
| `tests/memory/test_searcher.py` | Create | KeywordMemorySearcher 测试 |
| `tests/tools/test_memory_search.py` | Create | MemorySearchTool 测试 |
| `tests/side/test_memory_summary.py` | Create | MemorySummaryTask 测试 |

---

### Task 1: KeywordMemorySearcher 实现

**Files:**

- Create: `memory/searcher.py`
- Create: `tests/memory/test_searcher.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/memory/test_searcher.py` 应 FAIL。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/memory/test_searcher.py
import pytest
from pathlib import Path
import tempfile

def test_keyword_search_finds_match():
    """关键词搜索应找到匹配的长期记忆。"""
    from memory.searcher import KeywordMemorySearcher
    from memory.long_term import LongTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        ltm.write_topic("food.md", "## 食物偏好\n- 喜欢辣的食物\n- 会做爆炒腊肉",
                        {"name": "食物偏好", "description": "用户的食物偏好", "type": "user"})
        ltm.update_index()
        searcher = KeywordMemorySearcher(ltm, short_term=None)
        results = searcher.search("辣的食物")
        assert len(results) > 0
        assert "辣" in results[0].content

def test_keyword_search_returns_empty_for_no_match():
    """无匹配时应返回空列表。"""
    from memory.searcher import KeywordMemorySearcher
    from memory.long_term import LongTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        searcher = KeywordMemorySearcher(ltm, short_term=None)
        results = searcher.search("完全无关的关键词")
        assert results == []

def test_search_includes_short_term():
    """搜索应同时覆盖短期记忆。"""
    from memory.searcher import KeywordMemorySearcher
    from memory.long_term import LongTermMemory
    from memory.short_term import ShortTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        stm = ShortTermMemory(Path(d))
        stm.add_item("sess-1", "用户喜欢吃火锅", "pref_detection")
        searcher = KeywordMemorySearcher(ltm, short_term=stm)
        results = searcher.search("火锅")
        assert any("火锅" in r.content for r in results)
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/memory/test_searcher.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [x] **Step 3: Write minimal implementation**

```python
# memory/searcher.py
"""记忆搜索。"""
from __future__ import annotations

import logging
import re
from dataclasses import dataclass
from typing import Protocol, Any

logger = logging.getLogger(__name__)


@dataclass
class MemoryResult:
    source: str       # "short_term" | "long_term"
    content: str
    relevance: float
    topic: str | None = None


class MemorySearcher(Protocol):
    def search(self, query: str, limit: int = 5) -> list[MemoryResult]: ...


class KeywordMemorySearcher:
    """关键词匹配搜索。"""

    def __init__(self, long_term: Any, short_term: Any | None) -> None:
        self._long_term = long_term
        self._short_term = short_term

    def search(self, query: str, limit: int = 5) -> list[MemoryResult]:
        keywords = self._extract_keywords(query)
        if not keywords:
            return []

        results: list[MemoryResult] = []

        # 搜索长期记忆
        if self._long_term:
            for topic in self._long_term.list_topics():
                content = self._long_term.read_topic(topic["file"])
                score = self._score(content, keywords)
                if score > 0:
                    results.append(MemoryResult(
                        source="long_term",
                        content=self._extract_snippet(content, keywords),
                        relevance=score,
                        topic=topic["file"],
                    ))

        # 搜索短期记忆
        if self._short_term:
            for item in self._short_term.get_recent(limit=100):
                content = item.get("content", "")
                score = self._score(content, keywords)
                if score > 0:
                    results.append(MemoryResult(
                        source="short_term",
                        content=content,
                        relevance=score,
                    ))

        results.sort(key=lambda r: r.relevance, reverse=True)
        return results[:limit]

    @staticmethod
    def _extract_keywords(query: str) -> list[str]:
        # 简单分词：按空格和标点分割，过滤短词
        words = re.findall(r"[\w\u4e00-\u9fff]+", query.lower())
        return [w for w in words if len(w) >= 2]

    @staticmethod
    def _score(text: str, keywords: list[str]) -> float:
        text_lower = text.lower()
        hits = sum(1 for kw in keywords if kw in text_lower)
        return hits / len(keywords) if keywords else 0

    @staticmethod
    def _extract_snippet(content: str, keywords: list[str], context_chars: int = 200) -> str:
        content_lower = content.lower()
        for kw in keywords:
            idx = content_lower.find(kw)
            if idx >= 0:
                start = max(0, idx - context_chars)
                end = min(len(content), idx + len(kw) + context_chars)
                return content[start:end]
        return content[:context_chars]
```

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/memory/test_searcher.py -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add memory/searcher.py tests/memory/test_searcher.py
git commit -m "feat: KeywordMemorySearcher 实现（关键词搜索）"
```

---

### Task 2: MemorySearchTool 注册到 ToolRunner

**Files:**

- Create: `capability/tools/memory_search.py`
- Create: `tests/tools/test_memory_search.py`
- Modify: `cli/app.py`（注册工具）

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/tools/test_memory_search.py` 应 FAIL。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/tools/test_memory_search.py
import pytest
from unittest.mock import MagicMock

def test_memory_search_tool_returns_results():
    """MemorySearchTool 应返回搜索结果。"""
    from capability.tools.memory_search import MemorySearchTool
    mock_searcher = MagicMock()
    mock_searcher.search.return_value = [
        MagicMock(source="long_term", content="用户喜欢辣的食物", relevance=1.0, topic="food.md")
    ]
    tool = MemorySearchTool(mock_searcher)
    result = tool.execute({"query": "辣的食物"})
    assert "辣" in result
    mock_searcher.search.assert_called_once_with("辣的食物", limit=5)

def test_memory_search_tool_empty_query():
    """空查询应返回提示。"""
    from capability.tools.memory_search import MemorySearchTool
    tool = MemorySearchTool(MagicMock())
    result = tool.execute({"query": ""})
    assert "请输入" in result or "空" in result or len(result) > 0
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/tools/test_memory_search.py -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

```python
# capability/tools/memory_search.py
"""记忆搜索工具。"""
from __future__ import annotations

from typing import Any
from capability.tools.base import Tool


class MemorySearchTool(Tool):
    name = "memory_search"
    description = "搜索长期记忆，查找与当前话题相关的用户信息"
    parameters = {
        "type": "object",
        "properties": {
            "query": {"type": "string", "description": "搜索关键词"},
        },
        "required": ["query"],
    }

    def __init__(self, searcher: Any) -> None:
        self._searcher = searcher

    def execute(self, args: dict[str, Any]) -> str:
        query = args.get("query", "").strip()
        if not query:
            return "请输入搜索关键词。"
        results = self._searcher.search(query, limit=5)
        if not results:
            return "未找到相关记忆。"
        lines = []
        for r in results:
            prefix = f"[{r.source}]"
            if r.topic:
                prefix += f" ({r.topic})"
            lines.append(f"{prefix} {r.content[:200]}")
        return "\n".join(lines)
```

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/tools/test_memory_search.py -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add capability/tools/memory_search.py tests/tools/test_memory_search.py
git commit -m "feat: MemorySearchTool 实现（记忆搜索工具）"
```

---

### Task 3: MemorySummaryTask + BrixMemoryProvider 集成

**Files:**

- Create: `side/tasks/memory_summary.py`
- Create: `tests/side/test_memory_summary.py`
- Modify: `memory/provider.py`（集成 ShortTermMemory + LongTermMemory + searcher）
- Modify: `side/tasks/__init__.py`（注册 MemorySummaryTask）
- Modify: `cli/app.py`（注册 MemorySearchTool + MemorySummaryTask）

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/side/test_memory_summary.py` 应 FAIL。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/side/test_memory_summary.py
import pytest
from unittest.mock import MagicMock

@pytest.mark.asyncio
async def test_memory_summary_returns_summary():
    """MemorySummaryTask 应返回近期记忆摘要。"""
    from side.tasks.memory_summary import MemorySummaryTask
    task = MemorySummaryTask()
    mock_stm = MagicMock()
    mock_stm.get_recent.return_value = [
        {"content": "用户喜欢辣的食物", "source": "pref_detection", "created": "2026-05-29"},
        {"content": "用户天天自己做饭", "source": "pref_detection", "created": "2026-05-29"},
    ]
    mock_memory = MagicMock()
    mock_memory.short_term = mock_stm
    ctx = _make_ctx(
        llm_response="用户喜欢辣的食物，天天自己做饭，厨艺不错。",
        session_messages=[{"role": "user", "content": "你好"}],
    )
    import types
    ctx = types.SimpleNamespace(**{k: getattr(ctx, k) for k in ctx.__dataclass_fields__}, memory=mock_memory)
    result = await task.execute(ctx)
    assert result is not None
    assert len(result) > 0

@pytest.mark.asyncio
async def test_memory_summary_returns_none_when_empty():
    """无短期记忆时应返回 None。"""
    from side.tasks.memory_summary import MemorySummaryTask
    task = MemorySummaryTask()
    mock_stm = MagicMock()
    mock_stm.get_recent.return_value = []
    mock_memory = MagicMock()
    mock_memory.short_term = mock_stm
    ctx = _make_ctx(
        llm_response="摘要",
        session_messages=[{"role": "user", "content": "你好"}],
    )
    import types
    ctx = types.SimpleNamespace(**{k: getattr(ctx, k) for k in ctx.__dataclass_fields__}, memory=mock_memory)
    result = await task.execute(ctx)
    assert result is None
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_memory_summary.py -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

1. 创建 `side/tasks/memory_summary.py`
2. 改造 `memory/provider.py`：
   - 添加 `short_term` 和 `long_term` 属性
   - 初始化 `ShortTermMemory` 和 `LongTermMemory`
   - 创建 `KeywordMemorySearcher` 并暴露 `searcher` 属性
3. 修改 `side/tasks/__init__.py`：注册 MemorySummaryTask
4. 修改 `cli/app.py`：
   - 注册 MemorySearchTool（使用 `self._memory.searcher`）
   - 在 `_process_streaming` 中添加 memory_summary 调用

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/side/ tests/tools/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: MemorySummaryTask + BrixMemoryProvider 集成记忆系统"
```

---

### Task 4: 完整对话流程集成 + 端到端测试

**Files:**

- Modify: `cli/app.py`（完整集成）
- Modify: `tests/side/test_side_cli_integration.py`

**TDD_EVIDENCE:** `pytest tests/ -x -q` 全部 PASS。

- [x] **Step 1: Write the failing test**

在 `tests/side/test_side_cli_integration.py` 中添加集成测试，验证：
- MemorySearchTool 已注册
- memory_summary 在对话前调用
- pref_detection 结果写入短期记忆

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_side_cli_integration.py -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

完整集成：
1. `BrixMemoryProvider` 暴露 `short_term`, `long_term`, `searcher`
2. `cli/app.py` 注册 MemorySearchTool
3. `cli/app.py` 在 `_process_streaming` 中调用 memory_summary
4. pref_detection 通过 `ctx.memory.short_term.add_item()` 写入

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 完整记忆系统集成（搜索工具 + 短期记忆注入 + pref_detection 写入）"
```

# Side Task 修复 + 记忆基础设施 Implementation Plan

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** 修复所有未接入的 side task，实现短期记忆和长期记忆的基础设施类

**Milestone ref:** milestone-18 from claude-progress.json

**Architecture:** 删除冗余 voice_cleanup，改进 pref_detection 并将其结果写入新的 ShortTermMemory，改造 context_compress 为 token 触发，接入 session_summary。同时实现 ShortTermMemory 和 LongTermMemory 两个核心类。

**Tech Stack:** Python 3.11+, pytest, tiktoken (token 计数)

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `side/tasks/voice_cleanup.py` | Delete | 删除冗余 side task |
| `side/tasks/__init__.py` | Modify | 从 ALL_TASKS 移除 VoiceCleanupTask |
| `config/settings.yaml` | Modify | 补 LLMCleanupProcessor 默认配置 |
| `side/tasks/tool_summary.py` | Modify | 改为显示给用户而非丢弃 |
| `side/tasks/pref_detection.py` | Modify | 改进 prompt + 写入短期记忆 |
| `side/tasks/context_compress.py` | Modify | 改为 token 数触发 |
| `side/tasks/session_summary.py` | Modify | 接入 last_active_at 触发 |
| `memory/short_term.py` | Create | ShortTermMemory 类 |
| `memory/long_term.py` | Create | LongTermMemory 类 |
| `tests/memory/test_short_term.py` | Create | ShortTermMemory 测试 |
| `tests/memory/test_long_term.py` | Create | LongTermMemory 测试 |
| `tests/side/test_tasks.py` | Modify | 更新受影响的测试 |

---

### Task 1: 删除 voice_cleanup + LLMCleanupProcessor 可插拔

**Files:**

- Delete: `side/tasks/voice_cleanup.py`
- Modify: `side/tasks/__init__.py`
- Modify: `config/settings.yaml`
- Modify: `tests/side/test_tasks.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/side/` 应 FAIL（VoiceCleanupTask import 不存在）。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/side/test_tasks.py
def test_voice_cleanup_not_in_all_tasks():
    """VoiceCleanupTask 应已从 ALL_TASKS 中移除。"""
    from side.tasks import ALL_TASKS
    names = [t.name for t in ALL_TASKS]
    assert "voice_cleanup" not in names
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_tasks.py::test_voice_cleanup_not_in_all_tasks -x -q`
Expected: FAIL (VoiceCleanupTask 仍在 ALL_TASKS 中)

- [x] **Step 3: Write minimal implementation**

1. 删除 `side/tasks/voice_cleanup.py`
2. 修改 `side/tasks/__init__.py`：移除 `VoiceCleanupTask` 的 import 和实例化
3. 修改 `config/settings.yaml`：在 `voice:` 块中补充：
   ```yaml
   cleanup_enabled: false
   cleanup_model: ""
   cleanup_timeout: 0.8
   cleanup_min_length: 10
   ```

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/side/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor: 删除 voice_cleanup side task，补充 LLMCleanupProcessor 配置"
```

---

### Task 2: tool_summary 改为显示给用户

**Files:**

- Modify: `side/tasks/tool_summary.py`
- Modify: `tests/side/test_tasks.py`

**TDD_EVIDENCE:** Step 2 (RED): test 应 FAIL（返回类型不符合预期）。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/side/test_tasks.py
@pytest.mark.asyncio
async def test_tool_summary_returns_display_string():
    """tool_summary 应返回可显示的摘要字符串。"""
    from side.tasks.tool_summary import ToolSummaryTask
    task = ToolSummaryTask()
    ctx = _make_ctx(
        llm_response="Read config file",
        session_messages=[{"role": "user", "content": "help"}],
    )
    # 注入 tool 参数
    import types
    ctx_with_args = types.SimpleNamespace(
        **{k: getattr(ctx, k) for k in ctx.__dataclass_fields__},
        config={**dict(ctx.config), "_side_task_args": {
            "tool_name": "file_read",
            "tool_input": {"path": "config.yaml"},
            "tool_result": "content here",
        }},
    )
    result = await task.execute(ctx_with_args)
    assert isinstance(result, str)
    assert len(result) > 0
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_tasks.py::test_tool_summary_returns_display_string -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

修改 `side/tasks/tool_summary.py`：
- 当前实现已返回字符串，但 `fire_and_forget` 丢弃了结果
- 需要在 `cli/app.py` 中改为 `await run_task()` 并将结果通过 console.print 显示
- 或者保持 `fire_and_forget`，在 task 内部通过 hooks 显示

实际方案：保持 `fire_and_forget`，在 task 的 `execute()` 中通过 `ctx.hooks.fire("tool_summary", text=result)` 触发显示，CLI 层监听该事件并打印。

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/side/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: tool_summary 改为通过 hooks 显示工具摘要给用户"
```

---

### Task 3: ShortTermMemory 类实现

**Files:**

- Create: `memory/short_term.py`
- Create: `tests/memory/test_short_term.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/memory/test_short_term.py` 应 FAIL（模块不存在）。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/memory/test_short_term.py
import pytest
from pathlib import Path
import tempfile
import json

def test_add_and_get_items():
    """add_item 后 get_recent 应返回该 item。"""
    from memory.short_term import ShortTermMemory
    with tempfile.TemporaryDirectory() as d:
        stm = ShortTermMemory(Path(d))
        stm.add_item("sess-1", "用户喜欢辣的食物", "pref_detection", "用户说要吃爆炒腊肉")
        items = stm.get_recent(limit=10)
        assert len(items) == 1
        assert items[0]["content"] == "用户喜欢辣的食物"
        assert items[0]["source"] == "pref_detection"

def test_get_by_session():
    """get_by_session 应只返回指定 session 的 items。"""
    from memory.short_term import ShortTermMemory
    with tempfile.TemporaryDirectory() as d:
        stm = ShortTermMemory(Path(d))
        stm.add_item("sess-1", "item1", "pref_detection")
        stm.add_item("sess-2", "item2", "pref_detection")
        items = stm.get_by_session("sess-1")
        assert len(items) == 1
        assert items[0]["content"] == "item1"

def test_remove_items():
    """remove_items 应删除指定 id 的 items。"""
    from memory.short_term import ShortTermMemory
    with tempfile.TemporaryDirectory() as d:
        stm = ShortTermMemory(Path(d))
        stm.add_item("sess-1", "item1", "pref_detection")
        items = stm.get_recent()
        stm.remove_items([items[0]["id"]])
        assert len(stm.get_recent()) == 0

def test_cleanup_sessions():
    """cleanup_sessions 应删除指定 session 的文件。"""
    from memory.short_term import ShortTermMemory
    with tempfile.TemporaryDirectory() as d:
        stm = ShortTermMemory(Path(d))
        stm.add_item("sess-1", "item1", "pref_detection")
        stm.cleanup_sessions(["sess-1"])
        assert len(stm.get_recent()) == 0
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/memory/test_short_term.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [x] **Step 3: Write minimal implementation**

```python
# memory/short_term.py
"""短期记忆管理。"""
from __future__ import annotations

import json
import logging
import uuid
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

logger = logging.getLogger(__name__)


class ShortTermMemory:
    """管理短期记忆的读写。每个 session 一个 JSON 文件。"""

    def __init__(self, data_dir: Path) -> None:
        self._dir = data_dir / "short-term"
        self._dir.mkdir(parents=True, exist_ok=True)

    def _session_path(self, session_id: str) -> Path:
        return self._dir / f"{session_id}.json"

    def _load_session(self, session_id: str) -> dict:
        path = self._session_path(session_id)
        if not path.exists():
            return {"session_id": session_id, "created": "", "updated": "", "items": []}
        try:
            return json.loads(path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return {"session_id": session_id, "created": "", "updated": "", "items": []}

    def _save_session(self, data: dict) -> None:
        path = self._session_path(data["session_id"])
        path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")

    def add_item(self, session_id: str, content: str, source: str, context: str = "") -> None:
        data = self._load_session(session_id)
        now = datetime.now(timezone.utc).isoformat()
        if not data["created"]:
            data["created"] = now
        data["updated"] = now
        data["items"].append({
            "id": str(uuid.uuid4()),
            "content": content,
            "source": source,
            "created": now,
            "context": context,
        })
        self._save_session(data)

    def get_recent(self, limit: int = 50) -> list[dict[str, Any]]:
        all_items = []
        for p in sorted(self._dir.glob("*.json"), key=lambda f: f.stat().st_mtime, reverse=True):
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                all_items.extend(data.get("items", []))
            except (json.JSONDecodeError, OSError):
                continue
        all_items.sort(key=lambda x: x.get("created", ""), reverse=True)
        return all_items[:limit]

    def get_by_session(self, session_id: str) -> list[dict[str, Any]]:
        return self._load_session(session_id).get("items", [])

    def remove_items(self, item_ids: list[str]) -> None:
        ids = set(item_ids)
        for p in self._dir.glob("*.json"):
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                original_len = len(data.get("items", []))
                data["items"] = [i for i in data.get("items", []) if i["id"] not in ids]
                if len(data["items"]) != original_len:
                    self._save_session(data)
            except (json.JSONDecodeError, OSError):
                continue

    def cleanup_sessions(self, session_ids: list[str]) -> None:
        for sid in session_ids:
            path = self._session_path(sid)
            if path.exists():
                path.unlink()
```

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/memory/test_short_term.py -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add memory/short_term.py tests/memory/test_short_term.py
git commit -m "feat: ShortTermMemory 类实现（短期记忆读写）"
```

---

### Task 4: LongTermMemory 类实现

**Files:**

- Create: `memory/long_term.py`
- Create: `tests/memory/test_long_term.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/memory/test_long_term.py` 应 FAIL（模块不存在）。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/memory/test_long_term.py
import pytest
from pathlib import Path
import tempfile

def test_write_and_read_topic():
    """write_topic 后 read_topic 应返回内容。"""
    from memory.long_term import LongTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        ltm.write_topic("test_topic.md", "## 测试\n- 内容", {"name": "测试", "description": "测试主题", "type": "user"})
        content = ltm.read_topic("test_topic.md")
        assert "测试" in content

def test_list_topics():
    """list_topics 应返回 MEMORY.md 中的索引。"""
    from memory.long_term import LongTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        ltm.write_topic("topic1.md", "内容1", {"name": "主题1", "description": "描述1", "type": "user"})
        ltm.write_topic("topic2.md", "内容2", {"name": "主题2", "description": "描述2", "type": "user"})
        ltm.update_index()
        topics = ltm.list_topics()
        assert len(topics) == 2

def test_remove_topic():
    """remove_topic 应删除文件并更新索引。"""
    from memory.long_term import LongTermMemory
    with tempfile.TemporaryDirectory() as d:
        ltm = LongTermMemory(Path(d))
        ltm.write_topic("topic1.md", "内容", {"name": "主题", "description": "描述", "type": "user"})
        ltm.remove_topic("topic1.md")
        assert ltm.read_topic("topic1.md") == ""
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/memory/test_long_term.py -x -q`
Expected: FAIL (ModuleNotFoundError)

- [x] **Step 3: Write minimal implementation**

```python
# memory/long_term.py
"""长期记忆管理。"""
from __future__ import annotations

import logging
import re
from pathlib import Path
from typing import Any

logger = logging.getLogger(__name__)


class LongTermMemory:
    """管理长期记忆文件和 MEMORY.md 索引。"""

    def __init__(self, data_dir: Path) -> None:
        self._dir = data_dir / "long-term"
        self._dir.mkdir(parents=True, exist_ok=True)
        self._index_path = self._dir / "MEMORY.md"

    def list_topics(self) -> list[dict[str, str]]:
        if not self._index_path.exists():
            return []
        topics = []
        for line in self._index_path.read_text(encoding="utf-8").splitlines():
            m = re.match(r"^- \[(.+?)\]\((.+?)\) — (.+)$", line.strip())
            if m:
                topics.append({"name": m.group(1), "file": m.group(2), "description": m.group(3)})
        return topics

    def read_topic(self, topic_file: str) -> str:
        path = self._dir / topic_file
        if not path.exists():
            return ""
        return path.read_text(encoding="utf-8")

    def write_topic(self, topic_file: str, content: str, frontmatter: dict[str, str]) -> None:
        path = self._dir / topic_file
        fm = "\n".join(f"{k}: {v}" for k, v in frontmatter.items())
        full = f"---\n{fm}\n---\n\n{content}"
        path.write_text(full, encoding="utf-8")

    def update_index(self) -> None:
        entries = []
        for p in sorted(self._dir.glob("*.md")):
            if p.name == "MEMORY.md":
                continue
            try:
                text = p.read_text(encoding="utf-8")
                fm = self._parse_frontmatter(text)
                name = fm.get("name", p.stem)
                desc = fm.get("description", "")
                entries.append(f"- [{name}]({p.name}) — {desc}")
            except OSError:
                continue
        self._index_path.write_text("\n".join(entries) + "\n", encoding="utf-8")

    def remove_topic(self, topic_file: str) -> None:
        path = self._dir / topic_file
        if path.exists():
            path.unlink()
        self.update_index()

    @staticmethod
    def _parse_frontmatter(text: str) -> dict[str, str]:
        result = {}
        if not text.startswith("---"):
            return result
        end = text.find("---", 3)
        if end == -1:
            return result
        for line in text[3:end].strip().splitlines():
            if ":" in line:
                k, v = line.split(":", 1)
                result[k.strip()] = v.strip()
        return result
```

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/memory/test_long_term.py -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add memory/long_term.py tests/memory/test_long_term.py
git commit -m "feat: LongTermMemory 类实现（长期记忆文件 + 索引）"
```

---

### Task 5: pref_detection 改进 + 写入短期记忆

**Files:**

- Modify: `side/tasks/pref_detection.py`
- Modify: `tests/side/test_tasks.py`

**TDD_EVIDENCE:** Step 2 (RED): test 应 FAIL。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/side/test_tasks.py
@pytest.mark.asyncio
async def test_pref_detection_writes_to_short_term_memory():
    """pref_detection 应将检测到的偏好写入短期记忆。"""
    from side.tasks.pref_detection import PrefDetectionTask
    task = PrefDetectionTask()
    mock_stm = MagicMock()
    mock_memory = MagicMock()
    mock_memory.short_term = mock_stm
    ctx = _make_ctx(
        llm_response='[{"preference": "用户喜欢辣的食物", "context": "用户说要吃爆炒腊肉"}]',
        session_messages=[
            {"role": "user", "content": "我想吃爆炒腊肉"},
            {"role": "assistant", "content": "好的，很下饭！"},
        ],
    )
    ctx = types.SimpleNamespace(**{k: getattr(ctx, k) for k in ctx.__dataclass_fields__}, memory=mock_memory)
    await task.execute(ctx)
    mock_stm.add_item.assert_called_once()
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_tasks.py::test_pref_detection_writes_to_short_term_memory -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

修改 `side/tasks/pref_detection.py`：
1. 改进 prompt：只传 user/assistant 对话，去除 system 消息，格式化为清晰结构
2. 解析结果后，调用 `ctx.memory.short_term.add_item()` 写入短期记忆
3. 需要 `ctx.memory` 有 `short_term` 属性（在 milestone-19 的 BrixMemoryProvider 中集成）

注意：此 task 的完整集成依赖 milestone-19 中 BrixMemoryProvider 的改造。当前先实现 prompt 改进和写入逻辑的框架，实际 memory 属性在 milestone-19 接通。

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/side/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: pref_detection 改进 prompt + 写入短期记忆框架"
```

---

### Task 6: context_compress token 触发 + session_summary 接入

**Files:**

- Modify: `side/tasks/context_compress.py`
- Modify: `side/tasks/session_summary.py`
- Modify: `tests/side/test_tasks.py`

**TDD_EVIDENCE:** Step 2 (RED): test 应 FAIL。Step 4 (GREEN): 同命令应 PASS。

- [x] **Step 1: Write the failing test**

```python
# tests/side/test_tasks.py
@pytest.mark.asyncio
async def test_context_compress_triggers_on_token_count():
    """context_compress 应在 token 数超过阈值时触发。"""
    from side.tasks.context_compress import ContextCompressTask
    task = ContextCompressTask()
    # 构造大量消息使 token 数超过 max_context * 0.8
    big_content = "这是一段测试内容。" * 100  # ~500 chars ~125 tokens
    messages = [{"role": "user", "content": big_content}] * 200  # ~25000 tokens
    ctx = _make_ctx(
        llm_response="压缩后的摘要",
        session_messages=messages,
    )
    # 注入 max_context 配置
    ctx = types.SimpleNamespace(
        **{k: getattr(ctx, k) for k in ctx.__dataclass_fields__},
        config={**dict(ctx.config), "_side_task_args": {"max_context": 20000}},
    )
    result = await task.execute(ctx)
    assert result is not None
    assert len(result) > 0

@pytest.mark.asyncio
async def test_session_summary_triggers_on_idle():
    """session_summary 应在空闲超过阈值时触发。"""
    from side.tasks.session_summary import SessionSummaryTask
    task = SessionSummaryTask()
    import time
    ctx = _make_ctx(
        llm_response="你之前在重构认证模块",
        session_messages=[
            {"role": "user", "content": "帮我重构认证模块"},
            {"role": "assistant", "content": "好的"},
        ],
    )
    ctx = types.SimpleNamespace(
        **{k: getattr(ctx, k) for k in ctx.__dataclass_fields__},
        config={**dict(ctx.config), "_side_task_args": {"last_active_at": time.time() - 600}},
    )
    result = await task.execute(ctx)
    assert result is not None
```

- [x] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/side/test_tasks.py::test_context_compress_triggers_on_token_count -x -q`
Expected: FAIL

- [x] **Step 3: Write minimal implementation**

修改 `side/tasks/context_compress.py`：
- 读取 `max_context` 从 `ctx.config["_side_task_args"]["max_context"]`
- 用 tiktoken 或 `len(text)//4` 估算当前 token 数
- token 数 ≥ max_context × 0.8 时触发压缩

修改 `side/tasks/session_summary.py`：
- 从 `ctx.config["_side_task_args"]["last_active_at"]` 读取时间戳
- 已有逻辑基本正确，只需确保 last_active_at 被正确传递

- [x] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/side/ -x -q`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: context_compress 改为 token 触发 + session_summary 接入"
```

# P0: Voice Foundation — VAD + STT + LLM Cleanup 文本输出

> **Harness note:** This plan is executed via `harness-execution` using the Orchestrator / Executor / Reviewer architecture. Each task goes through Executor (TDD implementation) → Spec Reviewer (compliance check) → Code Quality Reviewer (adversarial review). Only Code Quality Review PASS closes a task.

**Goal:** 建立语音模块基础框架，实现"说话 → 终端显示润色文本"的完整链路。

**Milestone ref:** milestone-12 from claude-progress.json

**Architecture:** Pipecat Pipeline 编排 VAD → STT → LLM Cleanup 三个 Processor，PyAudio 麦克风采集 16kHz PCM，语音结束后整段转录，LLM 润色后通过 callback 注入 Brix 事件总线。

**Tech Stack:** Pipecat, Silero VAD, faster-whisper, PyAudio, numpy, scipy

---

## File Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `capability/voice/__init__.py` | Create | 模块入口，导出 VoiceRuntime |
| `capability/voice/protocol.py` | Create | VoiceRuntime Protocol 定义 |
| `capability/voice/config.py` | Create | 语音模块配置（采样率、模型路径、阈值） |
| `capability/voice/pipeline/__init__.py` | Create | Pipeline 子模块 |
| `capability/voice/pipeline/frames.py` | Create | VoiceStateFrame, CleanedTextFrame 自定义帧 |
| `capability/voice/pipeline/builder.py` | Create | build_voice_pipeline() 组装函数 |
| `capability/voice/processors/__init__.py` | Create | Processors 子模块 |
| `capability/voice/processors/vad.py` | Create | VADProcessor — Silero VAD 包装 |
| `capability/voice/processors/stt.py` | Create | STTProcessor — faster-whisper 包装 |
| `capability/voice/processors/llm_cleanup.py` | Create | LLMCleanupProcessor — 语音润色 |
| `capability/voice/transport/__init__.py` | Create | Transport 子模块 |
| `capability/voice/transport/local_audio.py` | Create | LocalAudioTransport — PyAudio 麦克风/扬声器 |
| `capability/voice/runtime.py` | Create | VoiceRuntimeImpl — 生命周期管理 |
| `pyproject.toml` | Modify | 添加 voice optional dependencies |
| `tests/voice/__init__.py` | Create | 测试模块 |
| `tests/voice/test_vad.py` | Create | VADProcessor 测试 |
| `tests/voice/test_stt.py` | Create | STTProcessor 测试 |
| `tests/voice/test_llm_cleanup.py` | Create | LLMCleanupProcessor 测试 |
| `tests/voice/test_pipeline.py` | Create | Pipeline 集成测试 |
| `tests/voice/test_runtime.py` | Create | VoiceRuntimeImpl 测试 |

---

### Task 1: Voice Protocol + Config + Dependencies

**Files:**

- Create: `capability/voice/__init__.py`
- Create: `capability/voice/protocol.py`
- Create: `capability/voice/config.py`
- Modify: `pyproject.toml`
- Create: `tests/voice/__init__.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/voice/test_protocol.py` should FAIL (no tests yet). Step 4 (GREEN): `pytest tests/voice/test_protocol.py` should PASS with VoiceRuntime Protocol importable and instantiable.

- [x] **Step 1: Write the failing test**

```python
# tests/voice/test_protocol.py
"""VoiceRuntime Protocol 测试。"""

from capability.voice.protocol import VoiceRuntime


def test_voice_runtime_protocol_exists():
    """VoiceRuntime Protocol 可以导入。"""
    assert VoiceRuntime is not None


def test_voice_runtime_protocol_methods():
    """VoiceRuntime Protocol 定义了所有必需方法。"""
    import inspect
    methods = [name for name, _ in inspect.getmembers(VoiceRuntime, predicate=inspect.isfunction)]
    # Protocol 的方法通过 __protocol_attrs__ 或 dir() 获取
    attrs = dir(VoiceRuntime)
    assert "start" in attrs
    assert "stop" in attrs
    assert "is_running" in attrs
    assert "on_voice_input" in attrs
    assert "on_state_change" in attrs
```

```python
# tests/voice/test_config.py
"""语音模块配置测试。"""

from capability.voice.config import VoiceConfig


def test_voice_config_defaults():
    """VoiceConfig 有合理的默认值。"""
    cfg = VoiceConfig()
    assert cfg.sample_rate == 16000
    assert cfg.channels == 1
    assert cfg.chunk_samples == 512  # Silero VAD 对齐
    assert cfg.vad_threshold == 0.5
    assert cfg.stt_model == "small"
    assert cfg.stt_language == "zh"


def test_voice_config_from_dict():
    """VoiceConfig 可以从 dict 构造。"""
    cfg = VoiceConfig.from_dict({
        "voice": {
            "sample_rate": 44100,
            "stt_model": "large-v3",
        }
    })
    assert cfg.sample_rate == 44100
    assert cfg.stt_model == "large-v3"
    assert cfg.vad_threshold == 0.5  # 未指定的用默认值
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_protocol.py tests/voice/test_config.py -x`
Expected: FAIL with `ModuleNotFoundError: No module named 'capability.voice'`

- [x] **Step 3: Write minimal implementation**

```python
# capability/voice/__init__.py
"""语音模块 — Brix 的拔插式语音交互能力。"""

from capability.voice.protocol import VoiceRuntime

__all__ = ["VoiceRuntime"]
```

```python
# capability/voice/protocol.py
"""VoiceRuntime Protocol — CLI 只通过此接口与语音模块交互。"""

from __future__ import annotations

from typing import Callable, Protocol, runtime_checkable


@runtime_checkable
class VoiceRuntime(Protocol):
    """语音运行时协议。

    启动后，语音文本通过 on_voice_input 注册的回调注入 Brix 主系统。
    状态变化通过 on_state_change 回调通知。
    """

    async def start(self) -> None:
        """启动语音 pipeline（麦克风采集、VAD、STT 全链路）。"""
        ...

    async def stop(self) -> None:
        """停止语音 pipeline，释放音频资源。"""
        ...

    @property
    def is_running(self) -> bool:
        """当前是否在运行。"""
        ...

    def on_voice_input(self, callback: Callable[[str], None]) -> None:
        """注册回调：语音识别出最终文本后调用。文本已过 LLM Cleanup。"""
        ...

    def on_state_change(self, callback: Callable[[str], None]) -> None:
        """注册回调：状态变化（listening / speaking / idle / error）。"""
        ...
```

```python
# capability/voice/config.py
"""语音模块配置。"""

from __future__ import annotations

from dataclasses import dataclass, field


@dataclass
class VoiceConfig:
    """语音模块配置。"""

    # 音频采集
    sample_rate: int = 16000
    channels: int = 1
    chunk_samples: int = 512  # Silero VAD 要求 512/1024/1536

    # VAD
    vad_threshold: float = 0.5
    min_speech_ms: int = 250
    min_silence_ms: int = 500

    # STT
    stt_model: str = "small"
    stt_language: str = "zh"
    stt_device: str = "cpu"
    stt_compute_type: str = "int8"

    # LLM Cleanup
    cleanup_enabled: bool = True
    cleanup_model: str = ""  # 空 = 使用 intent_model
    cleanup_timeout: float = 2.0
    cleanup_min_length: int = 5  # 短句跳过 cleanup

    # TTS (P1)
    tts_voice: str = "cosyvoice-v3-flash-default"
    tts_sample_rate: int = 24000
    tts_prefetch_ms: int = 80

    # 唤醒词 (P2)
    wake_word_model: str = "hey_brix"
    wake_threshold: float = 0.5
    wake_idle_timeout: float = 15.0

    # 连续对话 (P4)
    continuous_idle_timeout: float = 10.0

    @classmethod
    def from_dict(cls, data: dict) -> VoiceConfig:
        """从 Brix 配置 dict 构造。只取 voice 子项。"""
        voice_data = data.get("voice", {})
        cfg = cls()
        for key, value in voice_data.items():
            if hasattr(cfg, key):
                setattr(cfg, key, value)
        return cfg
```

```python
# tests/voice/__init__.py
"""语音模块测试。"""
```

- [x] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_protocol.py tests/voice/test_config.py -x`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add capability/voice/__init__.py capability/voice/protocol.py capability/voice/config.py tests/voice/__init__.py tests/voice/test_protocol.py tests/voice/test_config.py
git commit -m "feat(voice): VoiceRuntime Protocol + VoiceConfig 基础定义"
```

---

### Task 2: Custom Frames + LocalAudioTransport

**Files:**

- Create: `capability/voice/pipeline/__init__.py`
- Create: `capability/voice/pipeline/frames.py`
- Create: `capability/voice/transport/__init__.py`
- Create: `capability/voice/transport/local_audio.py`
- Create: `tests/voice/test_frames.py`
- Create: `tests/voice/test_local_audio.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/voice/test_frames.py tests/voice/test_local_audio.py` should FAIL. Step 4 (GREEN): same commands should PASS.

- [x] **Step 1: Write the failing test**

```python
# tests/voice/test_frames.py
"""自定义 Frame 类型测试。"""

from capability.voice.pipeline.frames import (
    VoiceStateFrame,
    CleanedTextFrame,
)


def test_voice_state_frame():
    """VoiceStateFrame 可以携带状态。"""
    frame = VoiceStateFrame(state="wake_detected")
    assert frame.state == "wake_detected"


def test_voice_state_frame_with_error():
    """VoiceStateFrame 可以携带错误信息。"""
    frame = VoiceStateFrame(state="processor_error", error="STT failed", processor="STTProcessor")
    assert frame.state == "processor_error"
    assert frame.error == "STT failed"
    assert frame.processor == "STTProcessor"


def test_cleaned_text_frame():
    """CleanedTextFrame 可以携带润色文本和原始文本。"""
    frame = CleanedTextFrame(text="你好世界", raw_text="嗯 你好世界")
    assert frame.text == "你好世界"
    assert frame.raw_text == "嗯 你好世界"
```

```python
# tests/voice/test_local_audio.py
"""LocalAudioTransport 测试。"""

import asyncio
from unittest.mock import MagicMock, patch, AsyncMock

import pytest

from capability.voice.transport.local_audio import LocalAudioTransport
from capability.voice.config import VoiceConfig


def test_local_audio_transport_init():
    """LocalAudioTransport 可以用 VoiceConfig 初始化。"""
    cfg = VoiceConfig()
    transport = LocalAudioTransport(cfg)
    assert transport is not None
    assert not transport.is_active


@pytest.mark.asyncio
async def test_local_audio_transport_start_stop():
    """LocalAudioTransport 可以启动和停止。"""
    cfg = VoiceConfig()
    transport = LocalAudioTransport(cfg)

    with patch("capability.voice.transport.local_audio.pyaudio") as mock_pa:
        mock_instance = MagicMock()
        mock_pa.PyAudio.return_value = mock_instance
        mock_stream = MagicMock()
        mock_instance.open.return_value = mock_stream

        await transport.start()
        assert transport.is_active

        await transport.stop()
        assert not transport.is_active
        mock_stream.stop_stream.assert_called_once()
        mock_stream.close.assert_called_once()
        mock_instance.terminate.assert_called_once()


@pytest.mark.asyncio
async def test_local_audio_transport_audio_queue():
    """麦克风回调将音频数据放入 asyncio Queue。"""
    cfg = VoiceConfig()
    transport = LocalAudioTransport(cfg)

    with patch("capability.voice.transport.local_audio.pyaudio") as mock_pa:
        mock_instance = MagicMock()
        mock_pa.PyAudio.return_value = mock_instance
        # 模拟 PyAudio 回调
        mock_stream = MagicMock()
        mock_instance.open.return_value = mock_stream

        await transport.start()

        # 模拟麦克风回调产生数据
        test_data = b"\x00\x01" * 256
        # PyAudio 的 callback 是在 __init__ 时注册的
        # 直接调用 transport 的内部回调
        transport._audio_callback(test_data, 256, None, None)

        # 从队列中取出数据
        data = await asyncio.wait_for(transport.audio_queue.get(), timeout=1.0)
        assert data == test_data

        await transport.stop()
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_frames.py tests/voice/test_local_audio.py -x`
Expected: FAIL with `ModuleNotFoundError: No module named 'capability.voice.pipeline'`

- [x] **Step 3: Write minimal implementation**

```python
# capability/voice/pipeline/__init__.py
"""Pipeline 子模块。"""
```

```python
# capability/voice/pipeline/frames.py
"""自定义 Frame 类型 — Voice 模块专用。"""

from __future__ import annotations

from dataclasses import dataclass, field


@dataclass
class VoiceStateFrame:
    """语音状态变化帧 — 通知 pipeline 外部状态。"""
    state: str  # "wake_detected" | "speech_start" | "speech_end" | "processing" | "speaking" | "error"
    error: str = ""
    processor: str = ""


@dataclass
class CleanedTextFrame:
    """LLM Cleanup 后的干净文本帧。"""
    text: str
    raw_text: str = ""  # 原始识别文本，用于调试
```

```python
# capability/voice/transport/__init__.py
"""Transport 子模块。"""
```

```python
# capability/voice/transport/local_audio.py
"""LocalAudioTransport — PyAudio 麦克风/扬声器管理。

PyAudio 的回调在独立线程中运行，通过 asyncio.Queue 桥接到主 event loop。
"""

from __future__ import annotations

import asyncio
import logging
from typing import Any

import pyaudio

from capability.voice.config import VoiceConfig

logger = logging.getLogger(__name__)


class LocalAudioTransport:
    """本地音频传输层 — 管理 PyAudio 麦克风输入。"""

    def __init__(self, config: VoiceConfig) -> None:
        self._config = config
        self._pa: pyaudio.PyAudio | None = None
        self._stream: pyaudio.Stream | None = None
        self._active = False
        self.audio_queue: asyncio.Queue[bytes] = asyncio.Queue(maxsize=100)

    @property
    def is_active(self) -> bool:
        return self._active

    async def start(self) -> None:
        """启动麦克风采集。"""
        if self._active:
            return

        self._pa = pyaudio.PyAudio()
        self._stream = self._pa.open(
            format=pyaudio.paInt16,
            channels=self._config.channels,
            rate=self._config.sample_rate,
            input=True,
            frames_per_buffer=self._config.chunk_samples,
            stream_callback=self._audio_callback,
        )
        self._active = True
        logger.info(
            "LocalAudioTransport started: %dHz, %d ch, %d samples/chunk",
            self._config.sample_rate,
            self._config.channels,
            self._config.chunk_samples,
        )

    async def stop(self) -> None:
        """停止麦克风采集，释放资源。"""
        if not self._active:
            return

        self._active = False
        if self._stream is not None:
            self._stream.stop_stream()
            self._stream.close()
            self._stream = None
        if self._pa is not None:
            self._pa.terminate()
            self._pa = None
        logger.info("LocalAudioTransport stopped")

    def _audio_callback(
        self,
        in_data: bytes | None,
        frame_count: int,
        time_info: Any,
        status: int,
    ) -> tuple[None, int]:
        """PyAudio 回调 — 在独立线程中运行。

        将音频数据放入 asyncio.Queue，由主 event loop 消费。
        """
        if in_data and self._active:
            try:
                self.audio_queue.put_nowait(in_data)
            except asyncio.QueueFull:
                # 队列满，丢弃最旧的数据
                try:
                    self.audio_queue.get_nowait()
                except asyncio.QueueEmpty:
                    pass
                try:
                    self.audio_queue.put_nowait(in_data)
                except asyncio.QueueFull:
                    pass
        return (None, pyaudio.paContinue)
```

- [x] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_frames.py tests/voice/test_local_audio.py -x`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add capability/voice/pipeline/__init__.py capability/voice/pipeline/frames.py capability/voice/transport/__init__.py capability/voice/transport/local_audio.py tests/voice/test_frames.py tests/voice/test_local_audio.py
git commit -m "feat(voice): 自定义 Frame 类型 + LocalAudioTransport 麦克风采集"
```

---

### Task 3: VADProcessor — Silero VAD

**Files:**

- Create: `capability/voice/processors/__init__.py`
- Create: `capability/voice/processors/vad.py`
- Create: `tests/voice/test_vad.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/voice/test_vad.py` should FAIL. Step 4 (GREEN): same command should PASS.

- [x] **Step 1: Write the failing test**

```python
# tests/voice/test_vad.py
"""VADProcessor 测试。"""

import numpy as np
import pytest

from capability.voice.processors.vad import VADProcessor
from capability.voice.pipeline.frames import VoiceStateFrame


def make_silence(samples=512):
    """生成静音 int16 PCM 数据。"""
    return np.zeros(samples, dtype=np.int16).tobytes()


def make_speech(samples=512):
    """生成模拟语音 int16 PCM 数据（随机噪声）。"""
    rng = np.random.RandomState(42)
    return (rng.randn(samples) * 5000).astype(np.int16).tobytes()


def test_vad_processor_init():
    """VADProcessor 可以初始化。"""
    vad = VADProcessor(threshold=0.5)
    assert vad is not None
    assert not vad.is_speaking


def test_vad_detects_silence():
    """VADProcessor 对静音不标记为语音。"""
    vad = VADProcessor(threshold=0.5)
    # 静音不应触发 speech_start
    result_frames = []
    # 模拟 process_frame（同步版本用于测试）
    for _ in range(10):
        vad._process_audio_chunk(make_silence())
    assert not vad.is_speaking


def test_vad_detects_speech():
    """VADProcessor 对语音数据标记为语音。"""
    vad = VADProcessor(threshold=0.3)
    # 注入足够多的语音 chunk 让 VAD 模型启动
    for _ in range(20):
        vad._process_audio_chunk(make_speech())
    # 注：真实 Silero VAD 模型需要加载，这里测试逻辑流程
    # 实际集成测试需要 mock 或真实模型
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_vad.py -x`
Expected: FAIL with `ModuleNotFoundError: No module named 'capability.voice.processors'`

- [x] **Step 3: Write minimal implementation**

```python
# capability/voice/processors/__init__.py
"""Processors 子模块。"""
```

```python
# capability/voice/processors/vad.py
"""VADProcessor — Silero VAD 语音活动检测。

检测音频流中的语音段起止。
语音开始时输出 VoiceStateFrame(state="speech_start")。
语音结束时输出 VoiceStateFrame(state="speech_end")。
"""

from __future__ import annotations

import logging
from typing import Any

import numpy as np

from capability.voice.config import VoiceConfig
from capability.voice.pipeline.frames import VoiceStateFrame

logger = logging.getLogger(__name__)


class VADProcessor:
    """Silero VAD 语音活动检测处理器。"""

    def __init__(
        self,
        threshold: float = 0.5,
        min_speech_chunks: int = 5,    # 约 160ms @ 512 samples/16kHz
        min_silence_chunks: int = 10,   # 约 320ms @ 512 samples/16kHz
    ) -> None:
        self._threshold = threshold
        self._min_speech_chunks = min_speech_chunks
        self._min_silence_chunks = min_silence_chunks
        self._model: Any = None
        self._is_speaking = False
        self._speech_chunk_count = 0
        self._silence_chunk_count = 0
        self._audio_buffer = bytearray()
        self._frames_collected: list = []  # 收集的音频帧，用于后续 STT

    @property
    def is_speaking(self) -> bool:
        return self._is_speaking

    def _ensure_model(self) -> None:
        """懒加载 Silero VAD 模型。"""
        if self._model is None:
            try:
                from silero_vad import load_silero_vad
                self._model = load_silero_vad()
                logger.info("Silero VAD model loaded")
            except ImportError:
                logger.error("silero-vad not installed. Install with: pip install silero-vad")
                raise

    def _process_audio_chunk(self, audio_bytes: bytes) -> float:
        """处理单个音频 chunk，返回 VAD 置信度。

        仅供同步测试使用。异步场景用 process_frame。
        """
        self._ensure_model()
        audio_float = np.frombuffer(audio_bytes, dtype=np.int16).astype(np.float32) / 32768.0
        confidence = self._model(audio_float, sample_rate=16000)
        return float(confidence)

    def process_frame_sync(self, audio_bytes: bytes) -> list:
        """同步处理音频帧，返回产生的帧列表。

        用于测试。异步场景用 process_frame。
        """
        self._ensure_model()
        audio_float = np.frombuffer(audio_bytes, dtype=np.int16).astype(np.float32) / 32768.0
        confidence = float(self._model(audio_float, sample_rate=16000))
        frames = []

        if confidence > self._threshold:
            self._silence_chunk_count = 0
            if not self._is_speaking:
                self._speech_chunk_count += 1
                if self._speech_chunk_count >= self._min_speech_chunks:
                    self._is_speaking = True
                    frames.append(VoiceStateFrame(state="speech_start"))
                    logger.debug("VAD: speech_start (confidence=%.3f)", confidence)
            if self._is_speaking:
                self._frames_collected.append(audio_bytes)
        else:
            self._speech_chunk_count = 0
            if self._is_speaking:
                self._silence_chunk_count += 1
                if self._silence_chunk_count >= self._min_silence_chunks:
                    self._is_speaking = False
                    frames.append(VoiceStateFrame(state="speech_end"))
                    logger.debug("VAD: speech_end (silence chunks=%d)", self._silence_chunk_count)
            # 静音时丢弃音频帧

        return frames

    def get_collected_audio(self) -> bytes:
        """获取收集的语音音频数据并清空 buffer。"""
        audio = b"".join(self._frames_collected)
        self._frames_collected.clear()
        return audio

    def reset(self) -> None:
        """重置 VAD 状态。"""
        self._is_speaking = False
        self._speech_chunk_count = 0
        self._silence_chunk_count = 0
        self._frames_collected.clear()
```

- [x] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_vad.py -x`
Expected: PASS (注：需要安装 silero-vad，否则 skip)

- [x] **Step 5: Commit**

```bash
git add capability/voice/processors/__init__.py capability/voice/processors/vad.py tests/voice/test_vad.py
git commit -m "feat(voice): VADProcessor — Silero VAD 语音活动检测"
```

---

### Task 4: STTProcessor — faster-whisper

**Files:**

- Create: `capability/voice/processors/stt.py`
- Create: `tests/voice/test_stt.py`

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/voice/test_stt.py` should FAIL. Step 4 (GREEN): same command should PASS.

- [x] **Step 1: Write the failing test**

```python
# tests/voice/test_stt.py
"""STTProcessor 测试。"""

import numpy as np
import pytest
from unittest.mock import MagicMock, patch

from capability.voice.processors.stt import STTProcessor
from capability.voice.pipeline.frames import VoiceStateFrame, CleanedTextFrame


def make_audio_bytes(duration_ms=1000, sample_rate=16000):
    """生成模拟音频 int16 PCM 数据。"""
    samples = int(sample_rate * duration_ms / 1000)
    rng = np.random.RandomState(42)
    return (rng.randn(samples) * 5000).astype(np.int16).tobytes()


def test_stt_processor_init():
    """STTProcessor 可以初始化。"""
    stt = STTProcessor(model_name="small", language="zh", device="cpu")
    assert stt is not None
    assert stt._language == "zh"


def test_stt_transcribes_audio():
    """STTProcessor 对音频数据进行转录。"""
    mock_model = MagicMock()
    # 模拟 faster-whisper transcribe 返回
    mock_segment = MagicMock()
    mock_segment.text = "你好世界"
    mock_info = MagicMock()
    mock_info.language = "zh"
    mock_model.transcribe.return_value = ([mock_segment], mock_info)

    stt = STTProcessor(model_name="small", language="zh", device="cpu")
    stt._model = mock_model

    audio = make_audio_bytes(1000)
    result_frames = stt.transcribe_sync(audio)

    assert len(result_frames) == 1
    assert result_frames[0].text == "你好世界"
    mock_model.transcribe.assert_called_once()


def test_stt_empty_audio():
    """STTProcessor 对空转录结果不输出帧。"""
    mock_model = MagicMock()
    mock_segment = MagicMock()
    mock_segment.text = ""
    mock_info = MagicMock()
    mock_model.transcribe.return_value = ([mock_segment], mock_info)

    stt = STTProcessor(model_name="small", language="zh", device="cpu")
    stt._model = mock_model

    result_frames = stt.transcribe_sync(b"")
    assert len(result_frames) == 0


def test_stt_voice_end_triggers_transcribe():
    """收到 speech_end 帧时触发转录。"""
    mock_model = MagicMock()
    mock_segment = MagicMock()
    mock_segment.text = "测试文本"
    mock_info = MagicMock()
    mock_model.transcribe.return_value = ([mock_segment], mock_info)

    stt = STTProcessor(model_name="small", language="zh", device="cpu")
    stt._model = mock_model
    stt._audio_buffer = make_audio_bytes(500)

    frames = stt.process_frame_sync(VoiceStateFrame(state="speech_end"))
    assert len(frames) == 1
    assert frames[0].text == "测试文本"
    assert stt._audio_buffer == b""
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_stt.py -x`
Expected: FAIL with `ModuleNotFoundError: No module named 'capability.voice.processors.stt'`

- [x] **Step 3: Write minimal implementation**

```python
# capability/voice/processors/stt.py
"""STTProcessor — faster-whisper 语音识别。

语音结束后整段转录（非实时流式），中文效果更好。
"""

from __future__ import annotations

import logging
from typing import Any

import numpy as np

from capability.voice.config import VoiceConfig
from capability.voice.pipeline.frames import CleanedTextFrame, VoiceStateFrame

logger = logging.getLogger(__name__)


class STTProcessor:
    """faster-whisper 语音识别处理器。"""

    def __init__(
        self,
        model_name: str = "small",
        language: str = "zh",
        device: str = "cpu",
        compute_type: str = "int8",
    ) -> None:
        self._model_name = model_name
        self._language = language
        self._device = device
        self._compute_type = compute_type
        self._model: Any = None
        self._audio_buffer = bytearray()

    def _ensure_model(self) -> None:
        """懒加载 faster-whisper 模型。"""
        if self._model is None:
            try:
                from faster_whisper import WhisperModel
                self._model = WhisperModel(
                    self._model_name,
                    device=self._device,
                    compute_type=self._compute_type,
                )
                logger.info(
                    "faster-whisper model loaded: %s (%s/%s)",
                    self._model_name, self._device, self._compute_type,
                )
            except ImportError:
                logger.error("faster-whisper not installed. Install with: pip install faster-whisper")
                raise

    def _transcribe_audio(self, audio_bytes: bytes) -> str:
        """转录音频数据为文本。"""
        if not audio_bytes:
            return ""

        audio_array = np.frombuffer(audio_bytes, dtype=np.int16).astype(np.float32) / 32768.0
        segments, info = self._model.transcribe(
            audio_array,
            language=self._language,
            beam_size=5,
            vad_filter=False,  # 已由 VAD 处理
        )
        text = " ".join(seg.text.strip() for seg in segments).strip()
        logger.debug("STT result: %s (lang=%s)", text[:50], info.language)
        return text

    def transcribe_sync(self, audio_bytes: bytes) -> list[CleanedTextFrame]:
        """同步转录，返回 CleanedTextFrame 列表。用于测试。"""
        self._ensure_model()
        text = self._transcribe_audio(audio_bytes)
        if text:
            return [CleanedTextFrame(text=text, raw_text=text)]
        return []

    def process_frame_sync(self, frame) -> list:
        """同步处理帧，返回产生的帧列表。用于测试。"""
        if isinstance(frame, VoiceStateFrame) and frame.state == "speech_end":
            audio = bytes(self._audio_buffer)
            self._audio_buffer.clear()
            text = self._transcribe_audio(audio)
            if text:
                return [CleanedTextFrame(text=text, raw_text=text)]
            return []
        elif hasattr(frame, "audio"):
            self._audio_buffer.extend(frame.audio)
        return []

    def reset(self) -> None:
        """重置 STT 状态。"""
        self._audio_buffer.clear()
```

- [x] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_stt.py -x`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add capability/voice/processors/stt.py tests/voice/test_stt.py
git commit -m "feat(voice): STTProcessor — faster-whisper 语音识别"
```

---

### Task 5: LLMCleanupProcessor + Pipeline Builder + VoiceRuntimeImpl

**Files:**

- Create: `capability/voice/processors/llm_cleanup.py`
- Create: `capability/voice/pipeline/builder.py`
- Create: `capability/voice/runtime.py`
- Create: `tests/voice/test_llm_cleanup.py`
- Create: `tests/voice/test_pipeline.py`
- Create: `tests/voice/test_runtime.py`
- Modify: `pyproject.toml` (add voice deps)

**TDD_EVIDENCE:** Step 2 (RED): `pytest tests/voice/test_llm_cleanup.py tests/voice/test_pipeline.py tests/voice/test_runtime.py` should FAIL. Step 4 (GREEN): same commands should PASS.

- [x] **Step 1: Write the failing test**

```python
# tests/voice/test_llm_cleanup.py
"""LLMCleanupProcessor 测试。"""

import pytest
from unittest.mock import AsyncMock

from capability.voice.processors.llm_cleanup import LLMCleanupProcessor
from capability.voice.pipeline.frames import CleanedTextFrame, VoiceStateFrame


@pytest.mark.asyncio
async def test_cleanup_cleans_text():
    """LLMCleanupProcessor 对口语化文本进行润色。"""
    mock_llm = AsyncMock(return_value="你好世界")
    cleanup = LLMCleanupProcessor(llm_fn=mock_llm)

    frame = CleanedTextFrame(text="嗯 你好世界", raw_text="嗯 你好世界")
    result = await cleanup.process(frame)

    assert result.text == "你好世界"
    assert result.raw_text == "嗯 你好世界"
    mock_llm.assert_called_once()


@pytest.mark.asyncio
async def test_cleanup_skips_short_text():
    """LLMCleanupProcessor 对短句跳过 Cleanup。"""
    mock_llm = AsyncMock()
    cleanup = LLMCleanupProcessor(llm_fn=mock_llm, min_length=5)

    frame = CleanedTextFrame(text="好的", raw_text="好的")
    result = await cleanup.process(frame)

    assert result.text == "好的"
    mock_llm.assert_not_called()


@pytest.mark.asyncio
async def test_cleanup_handles_empty_result():
    """LLMCleanupProcessor 处理 Cleanup 返回空字符串的情况。"""
    mock_llm = AsyncMock(return_value="")
    cleanup = LLMCleanupProcessor(llm_fn=mock_llm)

    frame = CleanedTextFrame(text="嗯啊嗯啊", raw_text="嗯啊嗯啊")
    result = await cleanup.process(frame)

    assert result is None  # 空结果返回 None


@pytest.mark.asyncio
async def test_cleanup_timeout_fallback():
    """LLMCleanupProcessor 超时时降级使用原始文本。"""
    import asyncio

    async def slow_llm(text):
        await asyncio.sleep(10)
        return "cleaned"

    cleanup = LLMCleanupProcessor(llm_fn=slow_llm, timeout=0.1)
    frame = CleanedTextFrame(text="嗯你好世界", raw_text="嗯你好世界")
    result = await cleanup.process(frame)

    # 超时降级：使用原始文本
    assert result.text == "嗯你好世界"


@pytest.mark.asyncio
async def test_cleanup_passes_non_text_frames():
    """LLMCleanupProcessor 对非 CleanedTextFrame 直接透传。"""
    mock_llm = AsyncMock()
    cleanup = LLMCleanupProcessor(llm_fn=mock_llm)

    frame = VoiceStateFrame(state="speech_start")
    result = await cleanup.process(frame)
    assert isinstance(result, VoiceStateFrame)
    assert result.state == "speech_start"
```

```python
# tests/voice/test_pipeline.py
"""Pipeline builder 测试。"""

import pytest
from unittest.mock import MagicMock, AsyncMock

from capability.voice.pipeline.builder import build_voice_pipeline


def test_build_voice_pipeline():
    """build_voice_pipeline 返回可配置的 Pipeline 对象。"""
    mock_transport = MagicMock()
    mock_vad = MagicMock()
    mock_stt = MagicMock()
    mock_cleanup = MagicMock()

    pipeline = build_voice_pipeline(
        audio_source=mock_transport,
        vad=mock_vad,
        stt=mock_stt,
        cleanup=mock_cleanup,
    )

    assert pipeline is not None
    # Pipeline 应该包含所有处理器
    assert len(pipeline.processors) == 4
```

```python
# tests/voice/test_runtime.py
"""VoiceRuntimeImpl 测试。"""

import asyncio
import pytest
from unittest.mock import MagicMock, AsyncMock, patch

from capability.voice.runtime import VoiceRuntimeImpl
from capability.voice.config import VoiceConfig


def test_runtime_init():
    """VoiceRuntimeImpl 可以初始化。"""
    cfg = VoiceConfig()
    hooks = MagicMock()
    llm_fn = AsyncMock()
    runtime = VoiceRuntimeImpl(config=cfg, hooks=hooks, llm_fn=llm_fn)
    assert runtime is not None
    assert not runtime.is_running


@pytest.mark.asyncio
async def test_runtime_start_stop():
    """VoiceRuntimeImpl 可以启动和停止。"""
    cfg = VoiceConfig()
    hooks = MagicMock()
    llm_fn = AsyncMock()

    runtime = VoiceRuntimeImpl(config=cfg, hooks=hooks, llm_fn=llm_fn)

    with patch.object(runtime, "_run_pipeline", new_callable=AsyncMock):
        await runtime.start()
        assert runtime.is_running

        await runtime.stop()
        assert not runtime.is_running


def test_runtime_on_voice_input_callback():
    """VoiceRuntimeImpl 可以注册语音输入回调。"""
    cfg = VoiceConfig()
    hooks = MagicMock()
    llm_fn = AsyncMock()
    runtime = VoiceRuntimeImpl(config=cfg, hooks=hooks, llm_fn=llm_fn)

    callback = MagicMock()
    runtime.on_voice_input(callback)
    assert runtime._on_voice_input == callback


def test_runtime_on_state_change_callback():
    """VoiceRuntimeImpl 可以注册状态变化回调。"""
    cfg = VoiceConfig()
    hooks = MagicMock()
    llm_fn = AsyncMock()
    runtime = VoiceRuntimeImpl(config=cfg, hooks=hooks, llm_fn=llm_fn)

    callback = MagicMock()
    runtime.on_state_change(callback)
    assert runtime._on_state_change == callback
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_llm_cleanup.py tests/voice/test_pipeline.py tests/voice/test_runtime.py -x`
Expected: FAIL with `ModuleNotFoundError`

- [x] **Step 3: Write minimal implementation**

```python
# capability/voice/processors/llm_cleanup.py
"""LLMCleanupProcessor — 利用 Brix 的 LLM 对语音识别文本做润色。

功能：去口头禅、补标点、纠错。
策略：短句跳过、超时降级、空结果过滤。
"""

from __future__ import annotations

import asyncio
import logging
from typing import Awaitable, Callable

from capability.voice.pipeline.frames import CleanedTextFrame, VoiceStateFrame

logger = logging.getLogger(__name__)

CLEANUP_SYSTEM_PROMPT = """你是语音文本润色器。你的唯一任务是将语音识别（ASR）的原始输出整理为干净的书面文本。

规则：
1. 去除口头禅和填充词：嗯、啊、呃、那个、就是说、然后的话、怎么说呢、对对对、是是是
2. 补全缺失的标点符号（句号、逗号、问号、感叹号）
3. 纠正明显的同音字/语音识别错误（如"在那"→"在哪"，根据上下文判断）
4. 保留原始语义和措辞，不改写内容，不添加信息
5. 如果原文已经是干净文本，直接原样返回
6. 如果原文为空或只有口头禅，返回空字符串

只返回润色后的文本。不要解释、不要加引号、不要加任何前缀。"""


class LLMCleanupProcessor:
    """LLM 语音润色处理器。"""

    def __init__(
        self,
        llm_fn: Callable[[str], Awaitable[str]],
        timeout: float = 2.0,
        min_length: int = 5,
    ) -> None:
        self._llm_fn = llm_fn
        self._timeout = timeout
        self._min_length = min_length

    async def process(self, frame):
        """处理帧。CleanedTextFrame → 润色后的 CleanedTextFrame 或 None。其他帧透传。"""
        if not isinstance(frame, CleanedTextFrame):
            return frame

        raw_text = frame.raw_text or frame.text

        # 短句跳过 Cleanup
        if len(raw_text) <= self._min_length:
            logger.debug("Cleanup skipped (short text): %s", raw_text)
            return frame

        # LLM Cleanup with timeout
        prompt = f"{CLEANUP_SYSTEM_PROMPT}\n\n原文：{raw_text}"
        try:
            cleaned = await asyncio.wait_for(
                self._llm_fn(prompt),
                timeout=self._timeout,
            )
        except asyncio.TimeoutError:
            logger.warning("Cleanup timeout (%.1fs), using raw text", self._timeout)
            return frame  # 降级：使用原始文本
        except Exception as exc:
            logger.error("Cleanup error: %s, using raw text", exc)
            return frame

        cleaned = cleaned.strip()

        # 空结果（纯口头禅）
        if not cleaned:
            logger.debug("Cleanup returned empty (filler words)")
            return None

        return CleanedTextFrame(text=cleaned, raw_text=raw_text)
```

```python
# capability/voice/pipeline/builder.py
"""Pipeline builder — 组装 Voice Pipeline。

目前使用简单的顺序处理框架。P1+ 阶段迁移到 Pipecat Pipeline。
"""

from __future__ import annotations

import asyncio
import logging
from typing import Any

logger = logging.getLogger(__name__)


class SimpleVoicePipeline:
    """简单的语音 Pipeline — 顺序执行各处理器。

    Pipecat 集成前的过渡实现。各处理器同步处理，通过 asyncio 桥接。
    """

    def __init__(
        self,
        audio_source: Any,
        vad: Any,
        stt: Any,
        cleanup: Any,
        on_final_text=None,
        hooks=None,
    ) -> None:
        self._audio_source = audio_source
        self._vad = vad
        self._stt = stt
        self._cleanup = cleanup
        self._on_final_text = on_final_text
        self._hooks = hooks
        self._running = False
        self._task: asyncio.Task | None = None

    @property
    def processors(self) -> list:
        return [self._audio_source, self._vad, self._stt, self._cleanup]

    async def start(self) -> None:
        """启动 Pipeline 处理循环。"""
        self._running = True
        self._task = asyncio.create_task(self._process_loop())
        logger.info("Voice pipeline started")

    async def stop(self) -> None:
        """停止 Pipeline。"""
        self._running = False
        if self._task is not None:
            self._task.cancel()
            try:
                await self._task
            except asyncio.CancelledError:
                pass
            self._task = None
        logger.info("Voice pipeline stopped")

    async def _process_loop(self) -> None:
        """主处理循环 — 从麦克风读取音频，经过 VAD → STT → Cleanup。"""
        try:
            while self._running:
                try:
                    audio_data = await asyncio.wait_for(
                        self._audio_source.audio_queue.get(),
                        timeout=0.1,
                    )
                except asyncio.TimeoutError:
                    continue

                # VAD 处理
                vad_frames = self._vad.process_frame_sync(audio_data)
                for frame in vad_frames:
                    if self._hooks:
                        self._hooks.fire("voice_state", state=frame.state)

                # STT 处理（speech_end 时触发转录）
                for frame in vad_frames:
                    stt_frames = self._stt.process_frame_sync(frame)
                    for stt_frame in stt_frames:
                        # Cleanup 处理
                        cleaned = await self._cleanup.process(stt_frame)
                        if cleaned is not None:
                            if self._hooks:
                                self._hooks.fire(
                                    "voice_input",
                                    text=cleaned.text,
                                    raw=cleaned.raw_text,
                                )
                            if self._on_final_text:
                                self._on_final_text(cleaned.text)

        except asyncio.CancelledError:
            logger.info("Voice pipeline loop cancelled")
        except Exception as exc:
            logger.error("Voice pipeline error: %s", exc, exc_info=True)
            if self._hooks:
                self._hooks.fire("voice_state", state="error", error=str(exc))


def build_voice_pipeline(
    audio_source: Any,
    vad: Any,
    stt: Any,
    cleanup: Any,
    on_final_text=None,
    hooks=None,
) -> SimpleVoicePipeline:
    """构建语音 Pipeline。"""
    return SimpleVoicePipeline(
        audio_source=audio_source,
        vad=vad,
        stt=stt,
        cleanup=cleanup,
        on_final_text=on_final_text,
        hooks=hooks,
    )
```

```python
# capability/voice/runtime.py
"""VoiceRuntimeImpl — 语音运行时实现。

管理语音 Pipeline 的完整生命周期：启动、运行、停止。
实现 VoiceRuntime Protocol。
"""

from __future__ import annotations

import asyncio
import logging
from typing import Any, Callable

from capability.voice.config import VoiceConfig
from capability.voice.pipeline.builder import build_voice_pipeline
from capability.voice.processors.llm_cleanup import LLMCleanupProcessor
from capability.voice.processors.stt import STTProcessor
from capability.voice.processors.vad import VADProcessor
from capability.voice.protocol import VoiceRuntime
from capability.voice.transport.local_audio import LocalAudioTransport

logger = logging.getLogger(__name__)


class VoiceRuntimeImpl:
    """语音运行时实现 — 管理 Pipeline 生命周期。"""

    def __init__(
        self,
        config: VoiceConfig,
        hooks: Any,
        llm_fn: Callable,
    ) -> None:
        self._config = config
        self._hooks = hooks
        self._llm_fn = llm_fn
        self._running = False
        self._shutdown = False

        self._on_voice_input: Callable[[str], None] | None = None
        self._on_state_change: Callable[[str], None] | None = None

        # 组件（懒初始化）
        self._transport: LocalAudioTransport | None = None
        self._vad: VADProcessor | None = None
        self._stt: STTProcessor | None = None
        self._cleanup: LLMCleanupProcessor | None = None
        self._pipeline: Any = None

    @property
    def is_running(self) -> bool:
        return self._running

    def on_voice_input(self, callback: Callable[[str], None]) -> None:
        self._on_voice_input = callback

    def on_state_change(self, callback: Callable[[str], None]) -> None:
        self._on_state_change = callback

    async def start(self) -> None:
        """启动语音 Pipeline。"""
        if self._running:
            logger.warning("VoiceRuntime already running")
            return

        self._shutdown = False
        logger.info("Starting VoiceRuntime...")

        # 初始化组件
        self._transport = LocalAudioTransport(self._config)
        self._vad = VADProcessor(
            threshold=self._config.vad_threshold,
        )
        self._stt = STTProcessor(
            model_name=self._config.stt_model,
            language=self._config.stt_language,
            device=self._config.stt_device,
            compute_type=self._config.stt_compute_type,
        )
        self._cleanup = LLMCleanupProcessor(
            llm_fn=self._llm_fn,
            timeout=self._config.cleanup_timeout,
            min_length=self._config.cleanup_min_length,
        )

        # 构建 Pipeline
        self._pipeline = build_voice_pipeline(
            audio_source=self._transport,
            vad=self._vad,
            stt=self._stt,
            cleanup=self._cleanup,
            on_final_text=self._handle_final_text,
            hooks=self._hooks,
        )

        # 启动
        await self._transport.start()
        await self._pipeline.start()
        self._running = True

        self._hooks.fire("voice_state", state="idle")
        if self._on_state_change:
            self._on_state_change("idle")

        logger.info("VoiceRuntime started")

    async def stop(self) -> None:
        """优雅停止语音 Pipeline。

        严格遵循关闭顺序，避免资源泄漏：
        1. 设置关闭标志
        2. 停止 Pipeline 处理
        3. 停止音频采集
        4. 释放资源
        5. Fire idle hook
        """
        if not self._running:
            return

        self._shutdown = True
        logger.info("Stopping VoiceRuntime...")

        # 1. 停止 Pipeline
        if self._pipeline is not None:
            await self._pipeline.stop()
            self._pipeline = None

        # 2. 停止音频采集
        if self._transport is not None:
            await self._transport.stop()
            self._transport = None

        # 3. 清理组件引用
        self._vad = None
        self._stt = None
        self._cleanup = None

        self._running = False

        # 4. Fire hook
        self._hooks.fire("voice_state", state="idle")
        if self._on_state_change:
            self._on_state_change("idle")

        logger.info("VoiceRuntime stopped")

    def _handle_final_text(self, text: str) -> None:
        """Pipeline 回调 — 语音识别出最终文本。"""
        if self._shutdown:
            return
        if self._on_voice_input:
            self._on_voice_input(text)
```

- [x] **Step 4: Run test to verify it passes**

Run: `cd /Users/kuicao/Applications/Brix && python -m pytest tests/voice/test_llm_cleanup.py tests/voice/test_pipeline.py tests/voice/test_runtime.py -x`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add capability/voice/processors/llm_cleanup.py capability/voice/pipeline/builder.py capability/voice/runtime.py tests/voice/test_llm_cleanup.py tests/voice/test_pipeline.py tests/voice/test_runtime.py
git commit -m "feat(voice): LLMCleanupProcessor + Pipeline builder + VoiceRuntimeImpl"
```

---

## Milestone 12 Summary

完成此 milestone 后，语音模块可以：
1. 通过 PyAudio 采集麦克风音频（16kHz PCM）
2. Silero VAD 检测语音活动段
3. faster-whisper 语音结束后整段转录
4. LLM Cleanup 润色文本（去口头禅、补标点、纠错）
5. 通过 callback 将文本注入 Brix 主系统

验证方式：启动 VoiceRuntime → 对着麦克风说话 → 终端显示润色后的文本。
